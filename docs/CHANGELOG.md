# Changelog

Documentation-level changelog. For application code changes, see Git history once Phase 1 begins.

## [Unreleased] — Phase 0 Documentation

### Added
- 2026-05-10: **`migrations/README.md` aligned with canonical D-DB-010 deletion policy.** Closes the gap flagged in the previous entry: `migrations/README.md` line 19 had drifted from `docs/01-database/02-tables-by-module.md` §"Deletion policy". `stock_movement` removed from Class 1 list; `stock_movement` + `cost_read_log` added to Class 2 list with brief inline rationale matching the canonical doc. Added a one-line cross-reference at the top of the D-DB-010 section pointing readers to the canonical source. **No SQL changes. No migration changes. No source code changes. No tests run. No commands executed.** Files updated (2): `migrations/README.md`, `docs/CHANGELOG.md` (this entry). Sprint 2A validation readiness unchanged — still ready to proceed.
- 2026-05-10: **Phase 1 Sprint 2A pre-validation comment / D-DB-010 alignment fixes.** Three small documentation-layer corrections found in the pre-validation review (no SQL behavior changed; no migrations run; no source code or secrets touched).
  - **Migration 0005 + 0006 down-section comments rewritten** to admit the dev-only behavior. The previous comments said Class 2 tables were "intentionally left in place" but the down sections actually `DROP TABLE` them — that was true for Sprint 2A pre-launch dev (where dev DBs are throwaway and `db:migrate down && db:migrate up` cycles are supported) but the comment wording was misleading. New wording: "Sprint 2A pre-launch DEV-ONLY down section: drops [N] tables for clean local re-migration. Class 2 D-DB-010 data-loss-prevention applies AFTER Phase 1 launch / production deployment — at that point this section MUST become empty / forward-fix only. Do NOT run `db:migrate down` against staging or production." Down SQL itself is unchanged.
  - **`stock_movement` formally aligned as Class 2 in D-DB-010.** `docs/01-database/02-tables-by-module.md` §"Deletion policy" updated: `stock_movement` removed from Class 1 list, added to Class 2 list. Rationale (now in the doc): the table has no `deleted_at` column, so Class 1 (soft-delete pattern requires `deleted_at`) was not actually implementable as written; Class 2 matches the schema shape and the audit-trail semantics already locked for sibling tables `audit_log` + `cost_history`. The Sprint 2A migration 0006 BEFORE-DELETE trigger is the correct implementation; this entry is documentation alignment, not an architectural change. `cost_read_log` also added to Class 2 list (Phase 1 schema reservation; 7-year retention; was implicitly Class 2 via the BEFORE-DELETE trigger in migration 0005 but missing from the Phase 0 list).
  - **Migration 0006 inline rationale comment updated** to drop the "Class 1 vs Class 2 reconcile-at-sign-off" caveat (since the alignment is now locked) and reference the canonical D-DB-010 documentation date.
  - **Files updated (4):** `migrations/sql/1700000005000_pricing-costs-rls.sql` (down-section comment only — SQL behavior unchanged); `migrations/sql/1700000006000_warehouses-stock.sql` (down-section comment + inline `stock_movement` rationale comment — SQL behavior unchanged); `docs/01-database/02-tables-by-module.md` (D-DB-010 §"Deletion policy" Class 1/Class 2 lists); `docs/CHANGELOG.md` (this entry).
  - **Architectural impact: zero.** No SQL behavior changed. No table definitions modified. No triggers added or removed. No RLS policies touched. The Sprint 2A migration 0006 stock_movement Class 2 trigger remains active and unchanged. `cost_history` and `audit_log` Class 2 triggers (from Sprints 1+2A) remain active and unchanged.
  - **Known gap (out of allowed-file scope for this fix):** `migrations/README.md` line 19 also lists `stock_movement` under D-DB-010 Class 1. This file was outside the allowed-file list for this targeted fix, so the migrations/README.md inconsistency persists. The canonical Phase 0 source of truth (`docs/01-database/02-tables-by-module.md`) is now authoritative; `migrations/README.md` is a Sprint 1 summary that should be updated separately to match — flagged for the next allowed-file pass or for inclusion in the Sprint 2A sign-off PR scope.
  - **No tracker stats change.** **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Sprint 2A status:** ready for validation. PRE-1 hard gate (CI Lighthouse on Sprint 2A kickoff PR) still applies as the entry condition.
- 2026-05-10: **Phase 1 Sprint 2A delivered — Schema + RLS + Cost Privacy Foundation.** First post-Sprint-1 implementation work; week 2 of the 4-week Phase 1 budget. Sprint 2 was split per the pre-Sprint risk review into 2A (foundation) + 2B (operational); this entry covers 2A only. **Code-only delivery — no migrations executed against any database by Claude, no production deploy, no live secrets, no `pnpm install` re-run (lockfile from Sprint 1 retained).**
  - **Scope (per `15-phases/phase-1-acceptance.md` Must Have items):** CI workflow extended with Postgres 16.4 service container + `DATABASE_URL` job env + Step 11 `pnpm db:migrate up` + Step 12 `pnpm test:rls`; migration 0004 (10 catalog tables); migration 0005 (7 cost-bearing tables + RLS policies + Class 2 BEFORE-DELETE triggers + `app` schema with `current_user_id()` + `has_permission()` helpers + `app_user` Postgres role + `cost_read_log` schema reservation per phase-1-acceptance.md line 241 lock); migration 0006 (8 warehouse/stock tables + `stock_movement` Class 2 trigger); RLS test runner (`vitest.rls.config.ts` + `tests/rls/helpers.ts` + 51 RLS tests in `tests/rls/cost-privacy.spec.ts`); defense-in-depth Layer 2 (`redactCostFields` + `redactCostFieldsDeep`), Layer 3 (`api-token-scopes.ts` cost-bearing scope catalogue + dual-approval gate), Layer 4 (`costVisibility`), Layer 5 (`assertCanExportCost` + `exportCostMode`) — all in `lib/rbac/cost-privacy.ts` + `lib/rbac/api-token-scopes.ts` + 23 unit tests in `tests/unit/cost-privacy.test.ts`; CI grep checks #10/#11/#12 added (no bare SELECT cost fields outside finance paths; no SELECT \* on cost-bearing tables; no console.\* logging cost values) — both `.mjs` (canonical) and `.sh` (CI parity) updated.
  - **Files created (9):** `migrations/sql/1700000004000_catalog.sql`, `migrations/sql/1700000005000_pricing-costs-rls.sql`, `migrations/sql/1700000006000_warehouses-stock.sql`, `vitest.rls.config.ts`, `tests/rls/helpers.ts`, `tests/rls/cost-privacy.spec.ts`, `lib/rbac/cost-privacy.ts`, `lib/rbac/api-token-scopes.ts`, `tests/unit/cost-privacy.test.ts`.
  - **Files updated (5):** `README.md` (Sprint 2A kickoff banner triggers PRE-1 CI Lighthouse), `.github/workflows/ci.yml` (Postgres service + 2 new pipeline steps), `package.json` (added `test:rls` + `test:rls:watch` scripts), `scripts/ci/grep-checks.mjs` + `scripts/ci/grep-checks.sh` (3 new D-RBAC-001 cost-exposure checks).
  - **Locked architectural enforcement (CI now runs against real Postgres on every PR):** D-RBAC-001 (RLS at DB layer + 9-layer defense-in-depth helpers for layers 1-5; layers 6-9 deferred to Sprint 2B); D-DB-002 (every UUID PK uses `gen_random_uuid()`); D-DB-003 (every monetary column `numeric(12,2)`; rates `numeric(5,4)`); D-DB-010 (Class 2 BEFORE-DELETE triggers on variant_country_cost, variant_marketer_cost, cost_history, cost_read_log, stock_movement); D-COUNTRY-013 (admin-dashboard inventory authority via warehouse + stock_movement); D-COUNTRY-014 (no hardcoded country fields — all per-country data via `country_id` FK); D-PAY-002/003 (`profit_floor_rule` table reservation; margin floors stored as DATA in `numeric(5,4)`, never code constants); D-READY-002 (`pricing_rule.active boolean DEFAULT false`; `warehouse.archived_at` archive lifecycle).
  - **`cost_read_log` Phase 1 schema reservation only.** Per `phase-1-acceptance.md` line 241 lock: schema + retention metadata + `cost_read_log.read` permission slug (Super Admin + Security Lead only — Finance Admin EXCLUDED for conflict-of-interest). NO Phase 1 instrumentation; Phase 2 wires the cost-read interceptors per the locked decision.
  - **Restrictions honored:** No `pnpm install` re-run (lockfile retained from Sprint 1); no `.env.local` written; no live API keys / WhatsApp tokens / 1Password / payment credentials; no production migration applied; no production deployment; no expansion beyond Sprint 2A acceptance items; no `cost_read_log` instrumentation; no Redis cache (Sprint 2B); no X-Country-Context middleware (Sprint 2B); no multi-country switcher UX work (Sprint 2B); no live data admin pages (Sprint 3); no migrations 0007-0011 (Sprint 3); no defense layers 6-9 (Sprint 2B); no D-INFRA-010 threshold weakening.
  - **Tests authored (Sprint 2A — expected outcomes pending user `pnpm test:unit` / `pnpm db:migrate up` / `pnpm test:rls` runs):**
    - `tests/unit/cost-privacy.test.ts` — 23 unit tests covering Layers 2/3/4/5 helpers; expected PASS once user runs `pnpm test:unit`
    - `tests/rls/cost-privacy.spec.ts` — 51 RLS tests covering: 22 variant_country_cost SELECT (2 finance + 20 non-finance), 2 variant_country_cost INSERT, 15 variant_marketer_cost SELECT (5 with marketer_cost.read + 10 without), 2 cost_history SELECT, 6 Class 2 DELETE-block triggers (variant_country_cost, variant_marketer_cost, cost_history, cost_read_log, stock_movement, audit_log), 4 `app.has_permission()` helper assertions; expected PASS after `pnpm db:migrate up` against a local dev Postgres
    - Total Sprint 2A test count = 32 unit (9 prior + 23 new) + 51 RLS + 26 E2E (unchanged) = 109 tests
  - **🟡 PROPOSED (one item — does not block Sprint 2A sign-off):** Sprint 2A applies Class 2 BEFORE-DELETE trigger to `stock_movement` per the Sprint 2A prompt's explicit rule. D-DB-010 documentation classifies `stock_movement` as Class 1 (soft-delete). Class 2 is strictly stricter (DELETE forbidden) than Class 1 (soft-delete preferred but DELETE allowed); the trigger does not break Class 1 functionality. **Recommendation flagged for review at Sprint 2A sign-off:** D-DB-010 should be reviewed for whether `stock_movement` should be formally upgraded to Class 2, aligning the locked policy with the implementation. No D-DB-010 change made unilaterally — flagged proposal only.
  - **Architectural impact: zero net change.** All work conforms to `phase-1-acceptance.md`. No new decisions opened. No new permission slugs (cost_read_log.read already in role-permission-matrix.csv). No new override types. No schema deviation from migration plan. No D-INFRA-010 threshold weakening.
  - **PRE-1 hard gate active.** Sprint 2A kickoff PR triggers GitHub Actions to run the `lighthouse` job on `ubuntu-latest`. If green, Sprint 1's GREEN-CONDITIONAL flag clears to GREEN. If red, Sprint 2A halts; investigate without lowering D-INFRA-010 thresholds.
  - **Files updated (1):** `/docs/CHANGELOG.md` — this entry. Plus the report itself: `/docs/15-phases/phase-1-sprint-2a-report.md` (NEW).
  - **Tracker stats unchanged.** Stats remain: **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Next prompt expected:** Phase 1 Sprint 2B — Operational layer (Redis cache replacing stub + 7 invalidation triggers + cache-vs-RLS independence test + X-Country-Context middleware + multi-country UX + defense layers 6-9 + pen-test rehearsal).
- 2026-05-10: **Phase 1 Sprint 1 local validation pass — Gates 1–6 green; Gate 7 deferred to CI.** End-to-end validation on Windows confirmed Sprint 1 functional acceptance through six of seven gates: pnpm install (Gate 1, 743 packages, 2m 34s), Playwright Chromium install (Gate 2), all five static-check sub-gates (Gate 3 — format / typecheck / lint / lockfiles / grep), 9/9 unit tests (Gate 4, 1.17s), 39/39 static page build (Gate 5, 1.579s compile, 102 kB shared First Load JS), 26/26 E2E tests across chromium-mobile + chromium-desktop projects (Gate 6, 18.6s). Gate 7 (Lighthouse) hit a Windows-only `chrome-launcher@1.2.1` EPERM cleanup race during post-audit temp-directory removal — the audit itself ran successfully (Chrome loaded each page, all metrics computed) but `chrome-launcher` could not synchronously delete its Windows temp profile due to NTFS lazy-release / Windows Defender file-lock behavior. **Marked DEFERRED TO CI**: GitHub Actions `ubuntu-latest` is the canonical Lighthouse validation environment per `D-INFRA-010`; Linux does not exhibit the NTFS race. **No source code, package.json, .npmrc, lighthouserc, or threshold changes.** Sprint 1 sign-off rating: **🟢 GREEN-CONDITIONAL**, pending the first Sprint 2 PR that triggers the CI Lighthouse job.
  - **Files updated (3):** `docs/15-phases/phase-1-sprint-1-report.md` (added §12 "Local Validation Results" with per-gate table + chrome-launcher EPERM root-cause analysis + sign-off rating), `docs/CHANGELOG.md` (this entry), `README.md` (added Lighthouse Windows footnote in command table).
  - **Architectural impact: zero.** No Phase 0 decision changed. No `D-INFRA-010` threshold weakened. No `lighthouserc.json` / `lighthouserc.mobile.json` assertion or URL changes. No new permission slug, override type, or schema reservation. The `chrome-launcher` behavior is environmental tooling, not project source.
  - **Locked rules unchanged:** D-DB-001/002/003, D-COUNTRY-013/014, D-CSP-001/002/004, D-INFRA-006/007/008/009/010, D-OPS-010, D-PAY-002/003, D-RBAC-001, D-READY-002, D-SAFE-001.
  - **Tracker stats unchanged.** **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **No secrets touched.** No `.env*` written or read. No live payment / WhatsApp / 1Password credentials accessed. No production migration executed. No deployment.
  - **Next step:** Sprint 2 may begin after this documentation sign-off lands. The first Sprint 2 PR triggers `.github/workflows/ci.yml`, which runs the Lighthouse job on `ubuntu-latest`. Sprint 2 must NOT progress past its first task until that CI run shows green Lighthouse against the Sprint 1 scaffold pages.
- 2026-05-09: **Phase 1 Sprint 1 review fixes applied.** Five fixes from the read-only Sprint 1 review (no scope expansion; no installs; no tests run; no migrations executed; no secrets touched).
  - **Fix 1 (CRITICAL — ESLint flat-config wiring):** `eslint.config.mjs` rewritten to use `@eslint/eslintrc`'s `FlatCompat` so `next/core-web-vitals` is correctly extended into ESLint v9 flat config. Without this, eslint-plugin-next + react-hooks + jsx-a11y rules wouldn't fire and `pnpm lint` (CI step 7) would fail at runtime. Added `@eslint/eslintrc: ^3.2.0` to devDependencies. Locked tooling unchanged (D-INFRA-007 = ESLint + Prettier; Biome NOT used).
  - **Fix 2 (D-INFRA-010 mobile coverage):** Split Lighthouse CI into two passes that run sequentially. `lighthouserc.json` keeps desktop preset (customer ≥ 0.95 / admin ≥ 0.90); new `lighthouserc.mobile.json` adds mobile preset (customer ≥ 0.90 / admin ≥ 0.85). Both enforce LCP < 2.0s / CLS < 0.05 / TBT < 200ms (proxy for INP). New `pnpm test:lighthouse:desktop` + `pnpm test:lighthouse:mobile` scripts; orchestrator `pnpm test:lighthouse` runs both. CI job comment + README command table updated. Thresholds NOT weakened.
  - **Fix 3 (CI grep test robustness):** `scripts/ci/grep-checks.sh` hardened — both filter greps now use `grep -vE` (ERE consistency); comment-skip wraps `$pattern` in parens so multi-alternation patterns are bounded; D-PAY-002/003 margin pattern broadened from 4-decimal forms only (`0.2000`) to also catch `0.20`, `0.18`, `0.22`, `0.05`, and percent forms (`20%`, `18%`, `22%`, `5%`); early-exit aligned with full SCOPE (`app/` + `lib/` + `components/` + `middleware.ts`). Allow-list extended to skip `node_modules`. Documentation files (`docs/`) remain intentionally outside SCOPE.
  - **Fix 4 (Routing regex single source of truth):** `next.config.ts` now imports `languages` + `countries` from `lib/i18n/config.ts` and computes the locale URL regex at build time — adding a country requires editing `config.ts` only. `lighthouserc.json` + `lighthouserc.mobile.json` URL regex broadened from `(ar|en)-(sa|eg|iq)` to `[a-z]{2}-[a-z]{2}` (Phase 1 locale shape) with `_meta.url_pattern_note` documenting why. Supported locale list NOT changed. No countries added.
  - **Fix 5 (Report + changelog):** Addendum section §11 added to `docs/15-phases/phase-1-sprint-1-report.md` documenting the five fixes. This CHANGELOG entry.
  - **Files updated (8):** `eslint.config.mjs`, `package.json`, `next.config.ts`, `lighthouserc.json`, `scripts/ci/grep-checks.sh`, `.github/workflows/ci.yml`, `README.md`, `docs/15-phases/phase-1-sprint-1-report.md`, `docs/CHANGELOG.md` (this entry).
  - **Files created (1):** `lighthouserc.mobile.json`.
  - **Dependencies added (1):** `@eslint/eslintrc: ^3.2.0` (devDependencies).
  - **Architectural impact: zero.** No new Phase 0 decision opened. No locked rule changed. No scope expanded. Five robustness/correctness fixes targeting issues raised in the read-only Sprint 1 review.
  - **Tracker stats unchanged.** Stats remain: **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Validation pending:** Sprint 1 readiness gate now requires (a) `pnpm install`, (b) `pnpm typecheck`, (c) `pnpm lint`, (d) `pnpm ci:grep`, (e) `pnpm test:unit`, (f) `pnpm test:e2e` after `pnpm dev`, (g) optional `pnpm test:lighthouse`. All to be run by user — Claude continues to author files only until install + lockfile commit are approved.
- 2026-05-09: **Phase 1 Sprint 1 Foundation Scaffold delivered.** First implementation work after Phase 0 documentation close. **Code-only delivery — no production deploy, no live secrets, no migrations executed against any database.**
  - **Scope (per `15-phases/phase-1-acceptance.md` Must Have items):** Next.js 15 + App Router + TypeScript scaffold; pnpm + ESLint + Prettier + Vitest + Playwright + Lighthouse CI tooling wired (D-INFRA-006/007/008/009/010); 6-locale routing (`ar-sa` / `en-sa` / `ar-eg` / `en-eg` / `ar-iq` / `en-iq`) with RTL/LTR via `dir` attribute (12-public-site/03-locale-routing.md); Admin shell with sidebar + Country Context Switcher (D-CSP-001) — single-country stub user (KSA-only) — declared `country_scope_mode` on every admin route per D-CSP-002; Build Now, Activate When Ready UI primitives — `<StatusBadge>` (8 states) + `<ReadinessBadge>` (score-or-missing) per D-READY-002; locked database extensions migration 0001 (`pgcrypto` for `gen_random_uuid()` per D-DB-002 + `pg_trgm` + `vector` + `pg_stat_statements`); RBAC + audit foundation migration 0002 (`user`, `role`, `permission`, `role_permission`, `audit_log` with DELETE-blocking trigger per D-DB-010 Class 2, `system_setting`); geo + locale foundation migration 0003 (`country` with `business_min_margin numeric(5,4)` + `compliance_profile jsonb`, `locale`, `currency` with `decimals` column for IQD=0, `tax_setting`, `country_launch_readiness`); RBAC architectural stubs (`lib/rbac/page-classification.ts` + `lib/rbac/permission-cache-stub.ts` documenting 11 hard rules from D-CACHE-001); 9 CI grep tests in `scripts/ci/grep-checks.sh` enforcing D-DB-001/002/003 + D-CSP-002 + D-COUNTRY-013/014 + D-OPS-010 + D-PAY-002/003 + D-SAFE-001 at source level; 12-step CI pipeline at `.github/workflows/ci.yml`; sprint 1 unit tests (Vitest — locale config + `cn`) + E2E tests (Playwright — homepage 6-locale dir attribute + admin shell country_scope_mode declarations + admin noindex headers).
  - **Files created (47):**
    - **Root configs (12):** `package.json`, `.npmrc`, `.gitignore`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`, `.prettierrc.json`, `vitest.config.ts`, `playwright.config.ts`, `lighthouserc.json`, `.env.example`, `middleware.ts`, `README.md`
    - **CI scripts (3):** `.github/workflows/ci.yml`, `scripts/ci/no-mixed-lockfiles.sh`, `scripts/ci/grep-checks.sh`
    - **App router (10):** `app/layout.tsx`, `app/[locale]/layout.tsx`, `app/[locale]/page.tsx`, `app/[locale]/globals.css`, `app/[locale]/admin/layout.tsx`, `app/[locale]/admin/page.tsx`, `app/[locale]/admin/products/page.tsx`, `app/[locale]/admin/countries/page.tsx`, `app/[locale]/admin/warehouses/page.tsx`, `app/[locale]/admin/audit/page.tsx`
    - **i18n (4):** `lib/i18n/config.ts`, `lib/i18n/request.ts`, `messages/ar.json`, `messages/en.json`
    - **Components (4):** `components/ui/status-badge.tsx`, `components/ui/readiness-badge.tsx`, `components/admin/country-context-switcher.tsx`, `components/admin/admin-shell.tsx`
    - **RBAC stubs + utils (3):** `lib/cn.ts`, `lib/rbac/page-classification.ts`, `lib/rbac/permission-cache-stub.ts`
    - **Migrations (5):** `migrations/.config.cjs`, `migrations/README.md`, `migrations/sql/1700000001000_initial-extensions.sql`, `migrations/sql/1700000002000_system-rbac-audit.sql`, `migrations/sql/1700000003000_geo-locale.sql`
    - **Tests (3):** `tests/unit/example.test.ts`, `tests/e2e/homepage.spec.ts`, `tests/e2e/admin-shell.spec.ts`
    - **Reports (1):** `docs/15-phases/phase-1-sprint-1-report.md`
  - **Locked architectural enforcement (CI grep tests run on every PR):** `D-DB-001` no bare `order` (use `customer_orders`); `D-DB-002` no `uuid_generate_v4()` (use `gen_random_uuid()` from pgcrypto); `D-DB-003` no `float`/`double precision`/`real`/`money` for monetary columns (use `numeric(12,2)`); `D-CSP-002` every admin route declares `country_scope_mode`; `D-COUNTRY-013` no ERP provider hardcoding (`'odoo'`/`'netsuite'`/`'sap'`/`'oracle'`/`'dynamics'`); `D-COUNTRY-014` no per-country hardcoded fields (`price_sa`/`stock_eg`/etc); `D-OPS-010` no hardcoded courier names; `D-PAY-002`/`D-PAY-003` no literal margin floors in code; `D-SAFE-001` no forbidden safety phrasings.
  - **Restrictions honored:** No `pnpm install` executed (user reviews dependency list first); no `.env.local` written; no live API keys / WhatsApp tokens / payment credentials; no production migration applied; no production deployment; no expansion beyond `phase-1-acceptance.md` Must Have items. Documents Sprint 2+ wiring requirements at the end of `phase-1-sprint-1-report.md`.
  - **Tests authored (sprint 1 expected outcomes — not yet executed locally pending `pnpm install`):**
    - `tests/unit/example.test.ts` — 9 unit tests (locale config × 6 + `cn` × 3); expected PASS once dependencies installed
    - `tests/e2e/homepage.spec.ts` — 8 E2E tests (6 locale `dir`/`lang` attribute checks + sprint 1 banner visible + CTA disabled); expected PASS
    - `tests/e2e/admin-shell.spec.ts` — 5 E2E tests (admin renders + country_scope_mode declarations × 3 + noindex/cache-control headers); expected PASS
  - **Architectural impact: zero scope expansion.** All work conforms to `phase-1-acceptance.md`. No new decisions; no new permission slugs; no new override types; no schema deviation from migration 0006 plan; no new modules. Phase 1 Sprint 1 produces the **minimum viable scaffold** the rest of Phase 1 builds on.
  - **Files updated (1):** `/docs/CHANGELOG.md` — this entry.
  - **Tracker stats unchanged.** Stats remain: **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Next prompt expected:** Phase 1 Sprint 2 — wire dependencies (`pnpm install`), execute first migration locally, add Postgres service container to CI, expand admin pages to read live data, add RLS policies for cost privacy (D-RBAC-001 / D-RBAC-008), add cost privacy unit + integration tests, expand CI to include `pnpm test:lighthouse` reporting target, begin Country Context Switcher multi-country UX once a multi-country stub user is added.
- 2026-05-07: Initial `/docs` folder structure created based on approved Phase 0 Execution Plan.
- 2026-05-07: All Phase 0 documentation files created as drafts.
- 2026-05-07: Master Plan v4 archived and referenced from `15-phases/`.
- 2026-05-07: **Phase 0 Decisions & TODO Tracker** created at `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — single source of truth consolidating 137 decisions across 15 categories from all Phase 0 documentation. 27 critical Phase 1 blockers identified.
- 2026-05-07: **Project Timeline & Phase Requirements** added at `15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` — phase-by-phase scope, deliverables, dependencies, acceptance criteria, resource needs, risk summary, and immediate next actions. Aligns with Master Plan v4 and the Decisions Tracker.

### Changed
- 2026-05-09: **Phase 0 Close Package prepared — meeting agenda + summary + Phase 1 prerequisites.** Final close-out documentation before stakeholder sign-off meeting. **No architecture or scope changes.**
  - **Created `/docs/15-phases/phase-0-close-meeting-agenda.md`** — comprehensive close meeting agenda with 6 sections: (A) Meeting objective; (B) Required attendees with quorum rule (6 required + 4 optional roles); (C) 45 closed decisions catalogued by category (database/schema, country/RBAC/cost privacy, profit guardrails/payments, engineering tooling, hosting/backups/secrets, strategic modules, cross-cutting architecture, operating principles, privacy+inventory, documentation cleanup, schedule, operational ownership); (D) 8 remaining operational long-lead items in flight per Day-1 checklist + 1 internal CTO call (D-LAUNCH-007); (E) Sign-off checklist table with 6 stakeholder rows + 5 caveat-recording rules; (F) Go / No-Go decision options A/B/C with recommended outcome (B: GO WITH ACTIONS); meeting minutes template at end.
  - **Created `/docs/15-phases/phase-0-close-summary.md`** — Phase 0 final summary with 8 sections: what Phase 0 achieved (quantitative + qualitative outcomes); key locked decisions (highlights); cleanup completed (3 steps + 2 accuracy fixes); Phase 1 scope health (~117 Must Have + 83 Should Have + 51 Schema Reservation); remaining operational actions (9 items with owners + lead times); what Phase 1 will start with (sprint 1 + sprint 2-4 deliverables); what Phase 1 must NOT expand into (19 explicit don't-expand items); final recommendation (GO WITH ACTIONS).
  - **Updated `/docs/15-phases/phase-1-execution-plan.md`** — new "Phase 1 Start Prerequisites" section with 7 prerequisite checkboxes covering Phase 0 close meeting completion, stakeholder sign-offs, Day-1 actions initiated, engineering team allocated, Phase 1 starter prompt approved, repo/tooling decisions locked, no new scope added; hard rule: if any prerequisite is incomplete, sprint 1 does NOT start.
  - **Updated `/docs/CHANGELOG.md`** — this entry.
  - **No tracker stats change.** Stats remain: **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** No decision changed; no scope expanded. Three close-out documents produced for stakeholder review.
  - **Files updated (4):**
    - NEW: `/docs/15-phases/phase-0-close-meeting-agenda.md`
    - NEW: `/docs/15-phases/phase-0-close-summary.md`
    - UPDATED: `/docs/15-phases/phase-1-execution-plan.md` (Phase 1 Start Prerequisites)
    - UPDATED: `/docs/CHANGELOG.md` (this entry)
- 2026-05-09: **D-COUNTRY-013 ANSWERED — Phase 1 inventory authority = Admin Dashboard / Directus-style admin only.** Last internal architectural Phase 1 blocker closed. **No architecture or scope changes — confirms admin-first inventory pattern that matches Master Plan v4 intent.**
  - **D-COUNTRY-013 (Inventory authority):** **Admin Dashboard is the Phase 1 inventory source of truth.** The platform itself IS the inventory authority. Warehouse stock, product-country availability, stock adjustments, and stock reservations all managed from platform/admin dashboard. **No external ERP is required for Phase 1; no two-way ERP sync; no ERP dependency blocks Phase 1.** ERP integration deferred to Phase 10+ or until business requirements justify it; future ERP integration may be added through provider-agnostic integration patterns.
  - **Locked rules (10):**
    1. Admin Dashboard is the Phase 1 inventory authority.
    2. Stock changes audit-logged via `stock_movement` + `country_data_audit`.
    3. Stock country/warehouse-specific.
    4. Stock reservations handled inside the platform (not via ERP).
    5. Manual stock adjustments require reason + audit log.
    6. ERP integration is future-phase only.
    7. Do NOT hardcode any ERP provider — CI grep test rejects `'odoo'`, `'netsuite'`, `'sap'`, `'oracle'`, `'dynamics'` in stock-decision code paths.
    8. If ERP introduced later, platform must support sync status, conflict handling, audit trails (provider-agnostic adapter pattern).
    9. Existing n8n / WhatsApp workflows MUST read platform inventory, NOT an external ERP.
    10. Inventory updates respect Country Access Control (D-CSP-001) + RBAC.
  - **Reasons:** reduces Phase 1 complexity; avoids external ERP dependency before ecommerce flow is proven; fits Build Now, Activate When Ready (D-READY-002); keeps warehouse/stock workflow controlled and auditable; allows later ERP sync without blocking Phase 1 foundation.
  - **Files updated (6):**
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — D-COUNTRY-013 marked 🟢 Answered with full lock notes; stats refreshed (158 total / 45 answered / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers); Top 10 reshuffled — D-LAUNCH-007 (engineering team allocation) promoted into rank #8; rank #10 now reads "No further internal blockers" (remaining items are external-dependency long-lead operational items already initiated per Day-1 checklist)
    - `/docs/01-database/02-tables-by-module.md` — Module 4 Stock & Warehouses prefaced with inventory authority lock + ERP-agnostic guidance + cross-references to admin IA + readiness engine
    - `/docs/11-admin-ui/01-information-architecture.md` — Locked-workflows section adds inventory authority entry confirming Operations / Warehouses + Stock as the canonical admin surface; no ERP dependency
    - `/docs/27-readiness-engine/01-unified-readiness.md` — New "Inventory readiness — Phase 1 source of truth" section with 6 readiness checks (stock_present, thresholds_set, reservations_active, audit_log_active, no_erp_dependency Critical, country_scope_respected)
    - `/docs/15-phases/phase-1-acceptance.md` — New "Inventory authority (🟢 D-COUNTRY-013 locked)" Must Have section with 8 acceptance items (admin dashboard authority verified; CI grep rejects ERP hardcoding; stock_movement rows written; country_data_audit rows written; manual adjustment reason required; stock reservation in-platform; Country Access Control respected; n8n/WhatsApp documentation)
    - `/docs/15-phases/phase-1-execution-plan.md` — New "Inventory authority locked" section before "Tooling locked" with 8-bullet summary + CI grep test note + cross-reference to migration 0006
    - `/docs/CHANGELOG.md` — this entry
  - **Tracker stats:** **158 total / 45 answered / 0 proposed / 113 open / 4 open Phase 1 blockers / 35 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** D-COUNTRY-013 confirms the admin-first inventory pattern already implicit in Module 4 schema design + Master Plan v4 §28. No schema change; no new module; no new permission; no new override type. Module 4 (Stock & Warehouses) schema continues per migration 0006 unchanged.
  - **Significance:** This was the **last internal architectural Phase 1 blocker.** All 10 cross-phase architecture decisions + all 5 schema-shaping database decisions + all 4 country access control decisions + all 5 engineering tooling decisions + the 4 strategic enhancement modules + the 2 operational principles + the 4 documentation cleanup decisions are now closed. The remaining 4 open Phase 1 blockers are **all external-dependency long-lead operational items** (photography, copywriter, WhatsApp Meta, payment providers + brand assets + hosting budget memo + engineering team allocation) that are already in flight per the Day-1 checklist. Per D-LAUNCH-010, Phase 0 close requires only **initiation** of these items, not completion.
- 2026-05-09: **D-INFRA-007 + D-INFRA-008 + D-INFRA-009 + D-INFRA-010 ANSWERED — Phase 1 engineering-tooling stack fully locked.** Four fast tooling decisions closed in a single technical review. **No architecture or scope changes.** All four are operational/tooling locks aligned with the existing D-INFRA-006 (pnpm) decision.
  - **D-INFRA-007 (Linter + formatter):** **ESLint + Prettier.** Reasons: mature ecosystem; strong support for TypeScript / React / Next.js / Tailwind / accessibility / import rules / formatting; better plugin coverage than Biome for this project stage; familiar to most TypeScript developers; explicit and reviewable configuration. **Locked rules:** ESLint is the standard linter; Prettier is the standard formatter; CI fails on lint/format errors; Biome NOT in Phase 1 (revisit later only if ecosystem support clearly improves).
  - **D-INFRA-008 (Test framework):** **Vitest + Playwright.** Reasons: Vitest is fast and fits Vite/TypeScript/React-style testing; Playwright is strong for E2E, browser, admin UI, storefront, country selector, visual flows, regression testing; better fit than Jest+Cypress for this stack and multi-browser validation. **Locked rules:** Vitest for unit/integration tests; Playwright for E2E/browser tests; CI runs both layers; Phase 1 focuses on foundation tests only — not exhaustive future-module testing.
  - **D-INFRA-009 (Visual regression):** **Playwright snapshots for Phase 1.** Reasons: no extra SaaS dependency; works with Playwright (D-INFRA-008); good enough for Phase 1 design-system, RTL/LTR, admin shell, product page, country-context checks; in-repo snapshots are version-controlled with the code. **Locked rules:** Playwright snapshots are the Phase 1 baseline; snapshots stored in repo per Playwright conventions; CI fails on unexpected visual changes; Chromatic and Percy NOT introduced in Phase 1; reconsider hosted tools later only if design review workflow needs hosted approvals.
  - **D-INFRA-010 (Lighthouse CI thresholds):** **Per-surface thresholds locked.**
    - Customer-facing mobile ≥ 90 / desktop ≥ 95
    - Admin mobile ≥ 85 / desktop ≥ 90
    - CWV: LCP < 2.0s; INP < 200ms; CLS < 0.05
    - **Locked rules:** Lighthouse CI runs on Phase 1 representative pages; representative customer pages = homepage / PLP / PDP; representative admin pages = dashboard / products / country pricing/stock / warehouses; CI fails if thresholds drop below approved limits unless an approved `performance_budget_override` exists (reason ≥30 chars + 14d expiry + audit log + mandatory remediation plan); aligns with D-PERF-001 performance budget discipline (per `19-performance-growth/01-performance-engineering.md` §A.3 + §A.4).
  - **Files updated (6):**
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — All 4 decisions marked 🟢 Answered with full lock notes; stats refreshed (158 total / 44 answered / 114 open / 5 open Phase 1 blockers / 34 answered Phase 1 blockers); Top 10 reshuffled — D-COUNTRY-013 (Inventory authority — Directus admin only vs ERP) promoted into rank #10
    - `/docs/00-architecture/stack-decisions.md` — 4 stack rows updated from "TODO" to "✅ Confirmed 2026-05-09" with full locked rules per row
    - `/docs/16-quality/test-strategy.md` — Status banner updated; Layers table annotated with locked tools per layer; Phase 1 baseline section updated with D-INFRA-010 thresholds; TODO section marked Answered for tool decisions + visual regression service
    - `/docs/16-quality/visual-regression-strategy.md` — Status updated to "Tool locked 2026-05-09"; tool spec replaced with locked Playwright snapshots + 5 reasons + 5 locked rules; TODO section marked Answered for tool pick + Chromatic budget
    - `/docs/16-quality/performance-budgets.md` — Status updated; Targets section split into Lighthouse thresholds (per surface × device) + CWV targets + supplementary metrics; CI enforcement section added with Phase 1 representative pages + override path
    - `/docs/15-phases/phase-1-execution-plan.md` — Tooling locked section expanded from D-INFRA-006 only to all 5 (D-INFRA-006/007/008/009/010); new "Phase 1 sprint 1 CI pipeline" section with 12-step pipeline including all CI grep tests
    - `/docs/15-phases/phase-1-acceptance.md` — New "Engineering tooling (🟢 D-INFRA-006/007/008/009/010 locked 2026-05-09)" section under Operational with 6 acceptance items
    - `/docs/CHANGELOG.md` — this entry
  - **Tracker stats:** **158 total / 44 answered / 0 proposed / 114 open / 5 open Phase 1 blockers / 34 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** All four are operational tooling. No schema change; no new module; no new permission slug; no new override type (existing `performance_budget_override` from D-PERF-003 handles Lighthouse violation overrides). The Phase 1 CI stack is now fully specified and ready for sprint 1 wiring.
- 2026-05-09: **D-LAUNCH-013 ANSWERED — Internal DPO designation locked.** Privacy ownership decision closed before Phase 0 close. **No architecture or scope changes.**
  - **D-LAUNCH-013 (Privacy officer / DPO designation):** **Internal DPO designated.** Either Legal Lead (preferred — formal legal training) OR Operations Director as interim with explicit privacy responsibility + training. Interim rule applies until legal/privacy role is formalized.
  - **External DPO / privacy consultant** may be added later if: customer messaging volume increases / multi-country privacy complexity grows / WhatsApp marketing automation expands / regulatory review requires external support / management decides to outsource privacy operations.
  - **Internal DPO Phase 1+ responsibilities:** privacy governance during launch; privacy-impact (PIA-equivalent) decisions for new features touching customer PII; customer data export approvals (per `customer.export` permission); deletion/anonymization requests (per D-DB-010 Class 4 workflow); marketing consent governance (per `17-compliance/consent-records.md`); cross-country PDPL compliance coordination (KSA + Egypt + Iraq); regulator liaison for subject-access requests and breach notifications.
  - **Important caveat documented:** designation does NOT mean all privacy work is complete — it means **ownership is assigned**. Customer PII collection must continue to follow documented privacy rules in `17-compliance/`.
  - **Files updated (5):**
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — D-LAUNCH-013 marked 🟢 Answered with full lock notes; stats refreshed (158 total / 40 answered / 118 open / 9 open Phase 1 blockers / 30 answered Phase 1 blockers); Top 10 reshuffled — D-INFRA-007 (Linter + formatter ESLint+Prettier vs Biome) promoted into rank #10
    - `/docs/17-compliance/ksa-pdpl.md` — TODO "DPO designation" marked 🟢 Answered with cross-reference to `GLOSSARY.md`
    - `/docs/17-compliance/consent-records.md` — TODO "designate privacy officer" marked 🟢 Answered with cross-reference to `GLOSSARY.md` + `ksa-pdpl.md`
    - `/docs/GLOSSARY.md` — new "Data Protection Officer (DPO)" entry under D section with internal designation rules + responsibilities + external-DPO trigger conditions
    - `/docs/15-phases/phase-0-execution-plan.md` — Day-1 Parallel-Track Operational Actions extended with row #8 "DPO designation" (Legal owner; EOD Day 7 deadline; D-LAUNCH-013 reference)
    - `/docs/15-phases/phase-1-acceptance.md` — new "Privacy ownership (🟢 D-LAUNCH-013 locked 2026-05-09)" Must Have section with 4 acceptance items (DPO recorded with name+email+date; DPO involved in customer schema sign-off for migration 0007; DPO documented as approver in consent + customer.export workflows; PDPL compliance docs reviewed by DPO)
    - `/docs/CHANGELOG.md` — this entry
  - **Tracker stats:** **158 total / 40 answered / 0 proposed / 118 open / 9 open Phase 1 blockers / 30 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** D-LAUNCH-013 is operational privacy ownership. No schema change; no new module; no new permission slug. Customer schema (migration 0007) ready to populate once DPO recorded with named individual.
- 2026-05-09: **D-LAUNCH-010 + D-INFRA-006 ANSWERED — Phase 0 close target date locked + package manager locked.** Two fast operational/tooling decisions closed before Phase 0 close. **No architecture or scope changes.**
  - **D-LAUNCH-010 (Phase 0 close target date):** **End of week 3 from Phase 0 start.** Stakeholder sign-off meeting scheduled at end-of-week-3. **Important clarification:** Phase 0 close does NOT require completion of every external dependency — photography, WhatsApp Meta approval, payment provider approvals, content delivery, and brand asset production must be **initiated from Day 1** (per the Day-1 Parallel-Track Operational Actions in `phase-0-execution-plan.md`) but **completion tracks during Phase 1**, not Phase 0. Phase 0 close requires: stakeholder sign-off recorded; Phase 1 readiness confirmed (architecture spine locked + documentation cleanup complete + Day-1 actions kicked off + 12 operational decisions resolved); Phase 1 starter prompt approved. Risk P0-07 (Phase 0 sliding indefinitely) mitigated by hard close date.
  - **D-INFRA-006 (Package manager):** **pnpm.** Reasons: good fit for large Next.js/TypeScript project; faster installs than npm; more deterministic dependency management; efficient disk usage; strong monorepo/workspace support if platform expands; works well with CI when lockfile committed. **Locked rules:**
    1. pnpm is the standard package manager.
    2. `pnpm-lock.yaml` committed to repo.
    3. Do NOT mix npm/yarn/pnpm lockfiles — CI rejects PRs introducing `package-lock.json` or `yarn.lock`.
    4. CI uses `pnpm install --frozen-lockfile`.
    5. Developer onboarding docs mention pnpm as canonical install path.
    6. **Package manager choice does NOT authorize installing packages in Phase 0** — Phase 0 remains documentation-only per ongoing scope-discipline rule.
  - **Files updated (5):**
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — D-LAUNCH-010 + D-INFRA-006 marked 🟢 Answered with full lock notes; stats refreshed (158 total / 39 answered / 119 open / 10 open Phase 1 blockers / 29 answered Phase 1 blockers); Top 10 reshuffled — D-LAUNCH-013 (Privacy officer / DPO designation) promoted into rank #10
    - `/docs/15-phases/phase-0-execution-plan.md` — new "Phase 0 close target date" section before Acceptance criteria with the end-of-week-3 lock + 3 close requirements + clarification that external-dependency completion is NOT required
    - `/docs/15-phases/phase-1-execution-plan.md` — new "Tooling locked" section noting pnpm + pnpm-lock.yaml + CI behavior + migration commands
    - `/docs/00-architecture/stack-decisions.md` — Package manager row updated from "TODO: confirm pnpm vs npm" to "🟢 pnpm (Confirmed 2026-05-09 — D-INFRA-006 Answered)" with locked rules
    - `/docs/CHANGELOG.md` — this entry
  - **Tracker stats:** **158 total / 39 answered / 0 proposed / 119 open / 10 open Phase 1 blockers / 29 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** Both decisions are operational/tooling. No schema change; no new module; no new permission; no new override type. Existing references to `pnpm db:migrate` in `01-database/05-migration-plan.md` and `00-architecture/adr/ADR-027-migration-tool.md` are now formally backed by D-INFRA-006.
- 2026-05-09: **D-READY-002 — "Build Now, Activate When Ready" operating principle locked.** Cross-platform doctrine making explicit a previously-implicit pattern across the 27 modules. **No architectural change; no scope expansion; no schema change.** Existing per-module status enums (country.launch_status, landing_page.status, shipping_provider.status, payment_provider.status, safety_claim.approval_status, messaging_template.approval_status, customer_review.approval_status, etc.) remain in place — the principle elevates them to a unified doctrine.
  - **Core rule:** Configured ≠ active. Entities may exist in admin as `draft / pending_configuration / pending_approval / ready / paused / blocked / archived` but **only `active + readiness_passed` items may be customer-facing.**
  - **Applies across 19 entity types:** countries, products, country pricing, warehouse stock, shipping providers, shipping rate cards, payment providers, WhatsApp numbers, WhatsApp templates, auto replies, AI chat, landing pages, coupons, marketers, media assets, safety claims, certificates, reviews / Trust Layer, feature flags. Each entity type's status enum + activation requirements catalogued in `27-readiness-engine/01-unified-readiness.md` "Operating principle: Build Now, Activate When Ready" section.
  - **Hard rules locked (6):** No unfinished entity customer-facing; no default fallback hiding missing config; no hardcoded activation in code; no automatic activation after data entry; manual override requires approval + reason + audit log + expiry; all activation/deactivation actions audit-logged with actor + prior status + new status + reason + override_request_id (if applicable).
  - **Examples documented:**
    - Shipping: J&T can be added as `pending_configuration` or `manual_mode` before API/rate card finalized; cannot be used in checkout until provider `status='active'` AND active rate card AND delivery promise.
    - Payment: Stripe/Tap/Paymob can exist as `pending_approval` or `sandbox`; cannot appear to customers until `status='active'` AND credentials configured AND webhook signing secret set.
    - WhatsApp: Templates can be `draft/submitted/pending_meta_approval`; cannot send until Meta-approved + `active=true` + content reviewed.
    - Product: Master can exist globally; cannot be sold in a country until per-(product, country) readiness passes (price + stock + delivery promise + media + SEO + AI knowledge).
    - Landing Page: Can be `draft`/`pending_review`; cannot publish until quality gate + performance budget + profit guardrail + inventory guardrail + compliance check pass.
  - **Admin UI implications:** Every admin list/edit screen for an entity with status lifecycle MUST surface: status badge (color-coded), readiness score where applicable, missing requirements list with quick-fix links, activation button disabled until ready (with tooltip explanation), reason why blocked, last reviewed by, activated by, activated at.
  - **Phase placement:** Phase 1 documents principle + foundation modules implement status fields per existing schema + admin shows status/readiness for foundation modules; Phase 2+ checkout enforces only-active providers/products; Phase 4+ WhatsApp uses only approved+active templates/numbers; Phase 6+ landing pages full pre-publish gates; Phase 10 advanced readiness automation (nightly check + auto-blocked status on regression + alerts).
  - **Override path** for activating before readiness passes: existing 15-type override system handles this via type-specific overrides (`country_activation`, `landing_quality_gate_override`, `safety_claim_override`, etc.). No new override type needed.
  - **Files updated (10):**
    - `/docs/GLOSSARY.md` — "Build Now, Activate When Ready" entry with canonical statement + 8-state status table + cross-references
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — new Section 19 "Operating Principles" with D-READY-002 marked 🟢 Answered; stats refreshed (158 total / 37 answered / 0 proposed / 121 open / 12 open Phase 1 blockers / 27 answered Phase 1 blockers / 10 cross-phase architecture decisions answered)
    - `/docs/27-readiness-engine/01-unified-readiness.md` — new "Operating principle: Build Now, Activate When Ready" section before "Why a unified engine"; full per-module status enum mapping table (19 entity types); 5 examples; 6 hard rules; 5-phase placement table
    - `/docs/11-admin-ui/01-information-architecture.md` — new "Build Now, Activate When Ready — admin UI implications" section with 8 required UI elements per status-bearing entity; "no auto-activate" hard rule
    - `/docs/01-database/02-tables-by-module.md` — Conventions section: new "Status fields" rule documenting per-module status enums + canonical principle + CI grep guard against hardcoded status values in activation-decision code paths
    - `/docs/20-shipping-logistics/01-overview.md` — new Architecture principle 0 "Build Now, Activate When Ready" with shipping-specific activation requirements + J&T example
    - `/docs/05-payments/02-provider-abstraction.md` — new "Build Now, Activate When Ready" section before "Why" with payment-provider activation requirements + sandbox/pending_approval status
    - `/docs/06-whatsapp/02-templates.md` — Build Now, Activate When Ready callout at top with template approval lifecycle
    - `/docs/19-performance-growth/03-landing-pages.md` — new section D.0 "Build Now, Activate When Ready" before D.1 Pre-Publish Quality Gate; landing-page activation requirements + override path
    - `/docs/15-phases/phase-1-acceptance.md` — new "Build Now, Activate When Ready" acceptance section with 5 tests covering status badge rendering / activation button disabling / backend rejection of inactive rows / no auto-activation / audit log on activation
    - `/docs/CHANGELOG.md` — this entry
  - **Tracker stats refreshed:** **158 total / 37 answered / 0 proposed / 121 open / 12 open Phase 1 blockers / 27 answered Phase 1 blockers / 10 cross-phase architecture decisions answered.**
- 2026-05-09: **Phase 0 Cleanup — Final accuracy fixes (post Final Readiness Review).** Two minor documentation accuracy issues identified in the Final Phase 0 Readiness Review were fixed. **No architecture or scope changes; no locked decisions modified.**
  - **Phase 1 acceptance count claim corrected.** `15-phases/phase-1-acceptance.md` previously claimed "~80 Must Have items" in two locations, but the actual count is 117. Updated both locations to read "~117 Must Have items, down from 241 (51% reduction)" — accurately reflecting the genuine major reduction without overstating. No items moved; no scope change. The 117 list is genuinely Must Have because each item ties to a locked architectural decision.
  - **Stale cross-references to old performance-growth monolith updated** — 14 forward-facing cross-references across 8 files updated from `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` to the appropriate per-topic split file:
    - §A.3 (Performance budgets) references → `01-performance-engineering.md`
    - §B.4 (Auto Image Quality Scanner) references → `02-image-video.md`
    - §C.4 (Landing Page Builder admin) + §D.2 (Landing Page Score) + §D.6 (Compliance Check) references → `03-landing-pages.md`
    - §J (Permissions) + §K (Safety Rules) references → `09-permissions-and-safety-rules.md`
    - Broad references (Source / "see also" entries) → `00-index.md`
  - **Files updated (8):**
    - `/docs/15-phases/phase-1-acceptance.md` (count claim accuracy at lines 11 + 219)
    - `/docs/01-database/02-tables-by-module.md` (Module 23 Source reference → 00-index.md)
    - `/docs/15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (3 topic-specific references — performance budgets / Growth & Performance admin / Auto Image Quality Scanner)
    - `/docs/11-admin-ui/01-information-architecture.md` (2 references — Landing Page Builder §C.4 → 03-landing-pages.md; locked-workflow broad reference → 00-index.md)
    - `/docs/07-reporting/03-data-quality-issues.md` (Issue #21 §D.2 reference)
    - `/docs/25-safety-compliance/02-safety-claim-policy.md` (Source line + §D.6 inline reference)
    - `/docs/25-safety-compliance/04-certificates-policy.md` (§D.6 reference)
    - `/docs/10-overrides/01-override-types.md` (Source line §K reference → 09-permissions-and-safety-rules.md)
    - `/docs/03-rbac/02-permissions.md` (3 §J references → 09-permissions-and-safety-rules.md)
  - **References intentionally left unchanged:**
    - `CHANGELOG.md` historical entries (lines documenting the original split + prior cleanup steps) — these are historical records of the file's name at that time and should not be rewritten
    - `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` — the redirect stub itself intentionally references its old name in describing the redirect
    - `19-performance-growth/00-index.md:27` — the index intentionally documents the original filename when explaining the redirect stub
    - `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` historical "Updated:" lists in D-PERF-001/002/003 entries + D-DOC-001 entry that documents the split happening — these are historical "Notes" columns recording the file-as-it-was-at-the-time
  - **No tracker stats change.** Stats remain: **157 total / 36 answered / 0 proposed / 121 open / 12 open Phase 1 blockers / 27 answered Phase 1 blockers / 9 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** Final Phase 0 close readiness rating: **READY FOR PHASE 0 CLOSE** (was: GO WITH MINOR CLEANUP — minor cleanup now applied).
- 2026-05-09: **Phase 0 Cleanup — Step 3: Day-1 operational checklist + folder numbering gap documented + Module 24–27 ERD/RLS deferral noted.** Final lightweight cleanup pass before Phase 0 close. No architectural decision changed; no scope expansion; no locked decision reversed.
  - **Day-1 parallel-track operational checklist** added to `15-phases/phase-0-execution-plan.md`. Lists 7 long-lead actions that must be initiated on **Day 1 of Phase 0** (not Day 7 or 14) because their lead times are external (Meta approval, photographer booking, copywriter sourcing, payment-provider review cycles, CFO budget approval) and would otherwise become Phase 1 blockers AT Phase 1 start:
    - Photography brief sent to 3+ photographers (D-LAUNCH-011; 4–6 wk lead)
    - Arabic copywriter outreach (D-SEO-005; 1–2 wk to contract)
    - WhatsApp Business Cloud API submission (D-WA-001; 1–4 wk Meta approval)
    - KSA WhatsApp number procurement (D-COUNTRY-010; 1–4 wk)
    - Payment provider applications submitted in parallel (D-PAY-010; 1–4 wk per provider)
    - Brand assets audit (D-LAUNCH-005; 1–3 wk if rebuild needed)
    - Hosting budget memo to CFO (D-LAUNCH-006; 3–7 d for sign-off)
    - Each action has owner + Day-1 deadline + lead time + tracker decision ID
    - "Why Day-1 not Day-7" rationale included
    - "What is NOT Day-1" list (D-LAUNCH-007 / D-LAUNCH-010 / D-LAUNCH-013 / D-COUNTRY-013 / D-INFRA-006 — internal decisions or shorter lead time)
    - Operations Director owns daily standup for these 7 items for the duration of Phase 0
  - **Folder numbering gap documented** in `/docs/README.md`. Explains:
    - Folders 21, 22, 23 do not exist — intentional, reserved for future modules
    - Modules 24–27 (Decision Engine, Safety & Compliance, Trust Layer, Readiness Engine) are aligned with database Module 24–27 schema reservations
    - Renumbering 24–27 → 21–24 would break ~50+ cross-references with no architectural benefit
    - **Recommendation:** preserve the gap. If folders 21/22/23 are ever needed, assign at that time
  - **Modules 24–27 ERD/RLS deferral noted** in `01-database/01-erd.md` and `01-database/03-rls-policies.md`. Explicit notes:
    - Modules 24–27 currently have Phase 1 schema reservations / planning docs only
    - Full ERD cluster diagrams + detailed per-table RLS policies are deferred to **Phase 6 design window**, unless a Module 24–27 table becomes active earlier
    - **Phase 1 should not be blocked by full ERD/RLS detail for these future modules**
    - Existing RLS rules (country-scope template per D-CSP-001, cost privacy per D-RBAC-001, audit log denormalization per D-CSP-004, Class 1/2 deletion per D-DB-010) still apply to any active table including future Module 24–27 tables IF they go active early
    - Specific Class assignments for Module 24–27 tables documented: `safety_incident_report` is Class 2 (10-year retention, never delete); `customer_review`, `recommendation_event` are Class 1 soft-delete; `readiness_check_run` is Class 3 hard-delete after retention
  - **Files updated (4):**
    - `/docs/15-phases/phase-0-execution-plan.md` — Day-1 parallel-track operational checklist with 7 actions + rationale + tracking ownership
    - `/docs/README.md` — Folder numbering gap notice + "why Modules 24–27 should NOT be renumbered now" rationale
    - `/docs/01-database/01-erd.md` — Module 24–27 ERD expansion deferral note (Phase 6); existing clusters 1–8 remain authoritative
    - `/docs/01-database/03-rls-policies.md` — Module 24–27 detailed RLS deferral note (Phase 6); existing RLS rules still apply to any active table
    - `/docs/CHANGELOG.md` — this entry
  - **No tracker stats change.** Step 3 is purely operational + navigational documentation. No new decisions added; no decisions changed status. Tracker stats remain: **157 total / 36 answered / 0 proposed / 121 open / 12 open Phase 1 blockers / 27 answered Phase 1 blockers / 9 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** Every locked decision remains in force. Phase 0 cleanup is now complete (Steps 1 + 2 + 3). Remaining Phase 0 work is **stakeholder sign-off on the 12 operational long-lead decisions** — purely operational, not documentation.
- 2026-05-09: **Phase 0 Cleanup — Step 2: D-CACHE-001 locked + cost_read_log instrumentation moved to Phase 2 + override docs synced to 15 types + Approval vs Override glossary entry.** No architectural decision changed; all locked decisions remain in force. Step 2 closes the last 🟡 Proposed decision and resolves the largest remaining documentation ambiguity.
  - **D-CACHE-001 LOCKED (🟢 Answered):** Session-level Redis-backed permission cache for application-layer permission checks. **Hard rules** (in tracker + `03-rbac/03-scopes.md` §H.0):
    - Cache role permissions, country access, and field permissions for application-layer checks
    - Cache TTL = session length or shorter (recommend ≤60 minutes)
    - **Cache MUST NOT replace database RLS** — RLS remains the final enforcement layer
    - Cache invalidated on: role change / permission change / `user_country_access` grant/revoke/change / `country_scope.all` grant/revoke / user deactivation / session logout / time-limited grant expiry (7 triggers total)
    - Pub/sub invalidation events flush affected cache keys within 60s across all app instances
    - Cache MUST NEVER cache secrets (API tokens, payment credentials, webhook signing keys, 1Password references)
    - Cache MUST NOT expose cost fields without `cost.read` / `cost.export` (cache stores slug presence only; cost values come from DB through RLS)
    - **Sensitive actions bypass cache** (cost reads / override approvals / `restore.production` / `country_scope.all` grants / `sensitive_cleanup_override`) — explicit fresh DB-backed permission check
    - Denied access attempts STILL audit-logged regardless of cache state
    - Cache key structure: `permcache:user:{user_id}:session:{session_id}`
    - Cache miss falls back to DB query and re-caches result
    - Defense-in-depth layers: Auth middleware → Permission cache (cache or DB miss) → App-layer permission check → Sensitive-action re-check (DB) → RLS at DB layer (final enforcement) → Field-level permission check
  - **`cost_read_log` instrumentation MOVED TO PHASE 2 MANDATORY (locked 2026-05-09):** Resolves the prior ambiguity of "Phase 1 sprint 4 if time permits, else Phase 2." **Reason:** Phase 1 already enforces cost privacy through RLS + API serialization + UI hiding + export restrictions + audit on writes. Read-path cost logging is important but should not overload Phase 1 sprint 4. **Phase 1 ships:** schema/table only + retention docs + `cost_read_log.read` permission slug (Super Admin + Security Lead only; Finance Admin excluded to avoid conflict-of-interest); 7-year retention per Class 2 deletion policy (D-DB-010 — never hard delete). **Phase 2 ships:** mandatory instrumentation of every cost-bearing API endpoint; integration test asserting every cost-bearing endpoint writes exactly one `cost_read_log` row per request.
  - **Override docs synced to 15-type model (D-DOC-001 continuation):** Updated three previously-stub documents to acknowledge the current 15 override types (8 original + 5 from D-PERF-003 + 2 from Strategic Enhancements):
    - `10-overrides/05-anomaly-detection.md` — added per-type anomaly emphasis table covering all 15 types; sensitive_cleanup_override flagged as "highest scrutiny — every use is reviewed by Security Lead next day"
    - `10-overrides/06-override-runbook.md` — modal copy updated with per-type minimum reason length (10 chars baseline; 30 chars for the 5 D-PERF-003 + 2 Strategic Enhancement types); per-type auto-expiry windows table; server-side auto-revert behavior documented for the 5 D-PERF-003 types
    - `10-overrides/07-monthly-governance-review.md` — added 4 new review questions covering auto-revert tracking + sensitive_cleanup_override scrutiny + safety_claim_override promotion-to-formal + recommendation_override rule-drift signal; per-type review focus table
    - **No new override types introduced; no override logic changed.** Only references that still described the older 8-type model were updated.
  - **Approval vs Override clarification added to GLOSSARY.md:** New entry distinguishes:
    - **Approval** — A normal review/approval for a pending business action that does NOT necessarily break a system guardrail. Backed by `approval` table (Module 19). Reason optional; auto-expiry typically not applied; webhook optional.
    - **Override** — A controlled exception where a user bypasses or breaks a system guardrail. Backed by `override_request` table (Module 20). Reason mandatory (10 chars baseline; 30 chars for D-PERF-003 + Strategic Enhancement types); auto-expiry per-type window; webhook mandatory; server-side auto-revert on expiry for 5 of 15 types.
    - Quick rule of thumb: action allowed by policy but needs sign-off → Approval. Action breaks a policy and needs documented exception → Override.
    - Cross-references to `approval` table, `override_request` table, 15 override types in `10-overrides/01-override-types.md`, dual-approval rules, anomaly detection, runbook, and monthly governance review.
  - **Files updated (8):**
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — D-CACHE-001 marked 🟢 Answered with full lock rules; stats refreshed (157 total / 36 answered / 0 proposed / 12 open Phase 1 blockers / 27 answered Phase 1 blockers)
    - `/docs/03-rbac/03-scopes.md` — new section H.0 "Permission cache layer (🟢 D-CACHE-001 locked)" with architecture, 8 hard rules, 7-trigger invalidation matrix, Phase 1 deliverables, defense-in-depth layer diagram
    - `/docs/03-rbac/04-cost-privacy.md` — new "Permission cache rules" section + cost_read_log Phase 2 lock note
    - `/docs/15-phases/phase-1-acceptance.md` — new "Permission cache layer (🟢 D-CACHE-001 locked)" section with 11 acceptance items; Phase 2 prerequisites section adds cost_read_log Phase 2 mandatory note
    - `/docs/15-phases/phase-1-execution-plan.md` — new "Permission cache layer" + "cost_read_log instrumentation deferred to Phase 2" task lists
    - `/docs/15-phases/phase-1-schema-reservation-tracker.md` — Cost-read audit logging section reframed: Phase 1 schema only; Phase 2 mandatory instrumentation
    - `/docs/10-overrides/05-anomaly-detection.md` — synced to 15 types + per-type anomaly emphasis table
    - `/docs/10-overrides/06-override-runbook.md` — synced to 15 types + per-type minimum reason length + per-type auto-expiry windows + auto-revert behavior
    - `/docs/10-overrides/07-monthly-governance-review.md` — synced to 15 types + 4 new review questions + per-type review focus table
    - `/docs/GLOSSARY.md` — Approval vs Override clarification entry
  - **Tracker stats refreshed:** **157 total / 36 answered / 0 proposed / 121 open / 12 open Phase 1 blockers / 27 answered Phase 1 blockers / 9 cross-phase architecture decisions answered.** All previously 🟡 Proposed decisions are now 🟢 Answered.
  - **Architectural impact: zero.** Every locked decision (D-DB-001/002/003/010, D-INFRA-004/001/018, D-COUNTRY-001/002/014/015, D-RBAC-001, D-PAY-002/003, D-BKP-001/002, D-PERF-001/002/003/004, D-OPS-003/010, D-CSP-001/002/003/004, D-DEC-001, D-SAFE-001, D-TRUST-001, D-READY-001, D-OWN-001, D-DOC-001, D-PHASE1-001, D-RBAC-MATRIX-001, D-CACHE-001) remains in force. Documentation updated only; no architectural redesign.
- 2026-05-09: **Phase 0 Cleanup — Step 1: Phase 1 scope reduction + documentation split + role-permission CSV.** No architectural decision changed; all locked decisions remain in force. Per the Phase 0 Architecture Review (2026-05-09), the project was over-scoped at 241 Phase 1 acceptance items and the Performance/Cleanup/Landing/Alerts spec had grown to a 1226-line monolith. This cleanup pass reorganizes content without changing architecture.
  - **D-PHASE1-001 (Phase 1 acceptance scope reduction):** Reduced `phase-1-acceptance.md` from 241 items to ~80 Must Have items. Created two companion files:
    - `phase-1-should-have.md` — Useful but non-critical Phase 1 items (extended testing, edge cases, hard-delete tests, IQD precision tests, Country Access UI tests, shipping admin section structure, heavy-asset CI rules, alert sub-categories, cleanup framework documentation).
    - `phase-1-schema-reservation-tracker.md` — Pure schema reservations + docs-only items (Module 24/25/26/27 schemas, cross-cutting columns, cost_read_log, country denormalization snapshot columns, performance/cleanup/alerts table skeletons, staging safety + promotion checklist, Script Inventory placeholder).
    - Items deferred to later phases (AI chat lazy-load → Phase 3; landing builder UI → Phase 6; cleanup execution → Phase 10; etc.) listed at end of `phase-1-should-have.md` with target phase.
    - **No content deleted.** All 241 original items preserved across the 3 files.
  - **D-DOC-001 (Documentation cleanup and file splitting):** Split `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` (1226 lines) into 9 per-topic files plus an index:
    - `00-index.md` — table of contents + cross-references
    - `01-performance-engineering.md` (section A) — CDN, page rendering, performance budgets, Core Web Vitals, JavaScript control, Script Weight Governance
    - `02-image-video.md` (section B) — image pipeline, 6 required variants, video rules, Auto Image Quality Scanner
    - `03-landing-pages.md` (sections C + D) — Landing Page Growth + Advanced Intelligence (Pre-Publish Quality Gate, Score, Compliance, Profit/Inventory Guardrails, Adaptive Pages, Smart CTA)
    - `04-cache-strategy.md` (section E) — cache rules, static snapshots, country edge cache, SWR, smart invalidation
    - `05-data-maintenance.md` (section F) — retention rules, cleanup jobs, safe workflow, lifecycle states, archiving
    - `06-database-health.md` (section G) — DB Health Dashboard, partitioning, summary tables, materialized views
    - `07-alerts.md` (section H) — 8 alert categories + 2 new sub-categories from D-CSP-001 + D-SAFE-001 (now 10 sub-categories total), severity levels, alert fields, workflow, Performance Incident System
    - `08-feature-flags.md` (section I) — 16 suggested flags, attributes
    - `09-permissions-and-safety-rules.md` (sections J + K + L) — ~25 permission slugs + 18 hard safety rules + cross-references
    - Original `PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` preserved as redirect stub pointing to `00-index.md`. External references continue to resolve.
    - **No content deleted.** All sections preserved.
  - **D-RBAC-MATRIX-001 (Role × Permission CSV):** v1 CSV produced at `03-rbac/matrix/role-permission-matrix.csv` covering **24 roles × ~165 permission slugs** including all major slug groups (catalog / cost / pricing / stock / country / warehouse / order / customer / conversation / AI / auto-reply / messaging / marketer / coupon / landing-page / campaign / b2b / ops / maintenance / report / system / import / export / backup / restore / override / mobile_admin / performance / data_maintenance / alerts / db_health / decision / safety / certificate / review / trust_asset / warranty / batch_quality / readiness / country-access / shipping). Cell values use locked conventions (Y / N / scoped / dual_approval / read / own / view_alerts / partial). Used by migration 0002 `role_permission` seed + CI consistency test (Phase 1 sprint 2) + admin Permissions Inspector. Companion README updated with v1 documentation.
  - **D-CACHE-001 (Permission cache layer architecture, 🟡 Proposed):** Recommendation locked in: session-level Redis-backed permission cache (compute effective permissions on login; cache as bitmap or jsonb in Redis with TTL = session length; invalidate on role/permission/country-access change). Phase 1 sprint 1 deliverable to lock cache strategy + invalidation events. Not yet 🟢 Answered — pending Backend Lead + Security Lead final review.
  - **Files created (15):**
    - `/docs/15-phases/phase-1-should-have.md`
    - `/docs/15-phases/phase-1-schema-reservation-tracker.md`
    - `/docs/19-performance-growth/00-index.md`
    - `/docs/19-performance-growth/01-performance-engineering.md`
    - `/docs/19-performance-growth/02-image-video.md`
    - `/docs/19-performance-growth/03-landing-pages.md`
    - `/docs/19-performance-growth/04-cache-strategy.md`
    - `/docs/19-performance-growth/05-data-maintenance.md`
    - `/docs/19-performance-growth/06-database-health.md`
    - `/docs/19-performance-growth/07-alerts.md`
    - `/docs/19-performance-growth/08-feature-flags.md`
    - `/docs/19-performance-growth/09-permissions-and-safety-rules.md`
    - `/docs/03-rbac/matrix/role-permission-matrix.csv`
  - **Files updated (4):**
    - `/docs/15-phases/phase-1-acceptance.md` (reduced from 378 lines / 241 items to ~80 Must Have items; cross-links to Should Have + Schema Reservation Tracker)
    - `/docs/19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` (replaced 1226-line monolith with redirect stub)
    - `/docs/03-rbac/matrix/README.md` (added v1 CSV documentation)
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (added new Section 18 with 4 cleanup decisions; refreshed stats to 157 total / 35 answered / 1 proposed / 121 open / 13 open Phase 1 blockers / 26 answered Phase 1 blockers)
  - **Tracker stats refreshed:** **157 total / 35 answered / 1 proposed / 121 open / 13 open Phase 1 blockers / 26 answered Phase 1 blockers / 9 cross-phase architecture decisions answered.**
  - **Architectural impact: zero.** Every locked decision (D-DB-001/002/003/010, D-INFRA-004/001/018, D-COUNTRY-001/002/014/015, D-RBAC-001, D-PAY-002/003, D-BKP-001/002, D-PERF-001/002/003/004, D-OPS-003/010, D-CSP-001/002/003/004, D-DEC-001, D-SAFE-001, D-TRUST-001, D-READY-001, D-OWN-001) remains in force. Acceptance items reclassified only; documentation reorganized only; CSV created only.
- 2026-05-09: **Country Access Control — RBAC extension locked. D-CSP-001 + D-CSP-002 + D-CSP-003 + D-CSP-004 all ANSWERED.** Treats country scoping as an extension of the existing RBAC system (NOT a new module). Composes with role permissions + field permissions + active country selector to produce a four-layer access decision per request.
  - **D-CSP-001 (Country Access Control architecture):** RBAC extension. Canonical table `user_country_access` (Phase 1 schema reservation; functional UI Phase 6). Existing `user.country_scope` jsonb preserved as denormalized read cache, kept in sync via trigger. AND-composes with role permissions + field permissions. Session vars `app.current_user_country_scope` + `app.current_user_has_country_scope_all` populated from cache. Defense in depth: API check + RLS at DB layer. External marketer special case: country comes from `marketer.country_id`, NOT `user_country_access`. Country Manager role requires at least one active `user_country_access` row at assignment time.
  - **D-CSP-002 (Page classification rule, 🛑 Phase 1 blocker):** Every admin route declares `country_scope_mode`: `'scoped' | 'aware' | 'global'`. CI grep test enforces declaration; missing → CI fails. Page classification table locked covering 25 country-scoped pages, 9 country-aware pages, 18 global-only pages.
  - **D-CSP-003 (`country_scope.all` as permission slug):** Modeled as `country_scope.all` permission slug granted via `role_permission`. Default grants in migration 0002 seed: super_admin, finance_admin, read_only_auditor, developer_api_admin. Other roles get this only via explicit ad-hoc grant (audit-logged High severity). Composes cleanly with existing RBAC.
  - **D-CSP-004 (Country denormalization rule, 🛑 Phase 1 blocker):** Every leaf-level country-scoped table includes `country_id` (or `country_id_snapshot` for immutable historical records) directly. Phase 1 adds: `audit_log.entity_country_id` NULL; `order_line.country_id_snapshot`, `order_event.country_id_snapshot`, `cart_line.country_id`, `cart_event.country_id`, `payment_transaction.country_id_snapshot`, `refund.country_id_snapshot`, `recommendation_warning_log.country_id`. Drift mitigation: CHECK constraint or insert/update trigger asserting parent country match.
  - **6 new permission slugs:** `country_scope.all`, `country_access.read`, `country_access.write`, `country_access.audit.read`, `report.cross_country.read`, `export.cross_country`.
  - **9 new audit log event types:** `user_country_access.granted/revoked/modified`, `country_scope.all.granted/revoked` (High), `cross_country_export` (High), `cross_country_report_run`, `denied_cross_country_access_attempt` (High), `country_scoped_sensitive_data_edit` (High).
  - **3 new alert sub-categories** under existing `business`: >5 `denied_cross_country_access_attempt` for same user in 24h (High); `country_scope.all` granted to new user (High; notify Security Lead); cross-country export > 10,000 rows (Medium).
  - **`X-Country-Context` HTTP header convention** active per `02-api/01-conventions.md`. Country-scoped endpoint missing header → 400; header value not in user's allowed countries → 403 + audit log; `all` without `country_scope.all` → 403 + audit. Backend MUST NOT rely on UI hiding alone.
  - **RLS policy template** applied to every country-scoped table per `01-database/03-rls-policies.md`. Reads session vars `current_user_country_scope` + `current_user_has_country_scope_all`. CI test asserts every country-scoped table has matching policy.
  - **Admin top-bar Country Context Switcher** scaffold wired (Phase 1 read-only). Per-page badges: yellow strip for `scoped`, subtitle for `aware`, grayed-out for `global`, red/orange strip when "All Countries" selected. Single-country / multi-country / all-country user states fully specified.
  - **Phase placement:** Phase 1 = schema + spine + permission slugs + RLS template + top-bar scaffold + API header + CI checks. Phase 2 = checkout + order list filter. Phase 4 = WhatsApp + Customer Messaging country scoping. Phase 5 = marketer scope. Phase 6 = landing pages + Decision Engine + cross-country reports + grant/revoke admin UI. Phase 7 = payment + COD filter. Phase 8 = EG + IQ activation. Phase 9 = operations + warranty + service tickets. Phase 10 = grant-expiry automation + analytics dashboard + drift detection.
  - **Files updated (18):**
    - `/docs/03-rbac/03-scopes.md` — **major rewrite** from stub to full canonical spec (12 sections: Architecture, Layered access model, Canonical data model, "All Countries" slug, Country denormalization, Page classification, API convention, RLS strategy, Admin UX, Audit + alerts, Phase placement, Existing scopes preserved)
    - `/docs/03-rbac/01-roles.md` — Default `country_scope.all` grants (super_admin / finance_admin / read_only_auditor / developer_api_admin); Country Manager role enforcement; cache-sync pattern reference
    - `/docs/03-rbac/02-permissions.md` — Country Access Control permission group (6 new slugs)
    - `/docs/01-database/02-tables-by-module.md` — `user_country_access` table spec in Module 19; `audit_log.entity_country_id` column; cross-cutting Country denormalization columns table
    - `/docs/01-database/03-rls-policies.md` — Country-scope RLS policy template + session variable contract
    - `/docs/02-api/01-conventions.md` — `X-Country-Context` header convention + endpoint class behavior matrix
    - `/docs/11-admin-ui/01-information-architecture.md` — Country Context Switcher widget + page classification table + behavior matrix
    - `/docs/11-admin-ui/05-states.md` — Country Context badge states + switch confirmation modal states + permission-denied state extension
    - `/docs/04-ai/05-guardrails.md` — Cross-country mixing rules (7 hard rules)
    - `/docs/06-whatsapp/03-numbers.md` — Per-country isolation rules
    - `/docs/07-reporting/02-metric-definitions.md` — Country scope rule for metrics
    - `/docs/07-reporting/06-report-builder.md` — Country scope rules for single-country / cross-country reports + cross-country exports
    - `/docs/27-readiness-engine/01-unified-readiness.md` — Country Access Control readiness checks (4 new checks)
    - `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` — Section 17 added with 4 new decisions (D-CSP-001/002/003/004); stats refreshed to 153 total / 32 answered / 12 open Phase 1 blockers / 23 answered Phase 1 blockers / 9 cross-phase architecture decisions answered
    - `/docs/15-phases/phase-1-acceptance.md` — Country Access Control acceptance section (~25 tests covering canonical data model / permission slug / denormalization columns / page classification rule / API convention / RLS template / admin top-bar scaffold / Country Manager role enforcement / alert sub-categories)
    - `/docs/15-phases/phase-1-execution-plan.md` — Schema reservation tasks distributed across migrations 0002 / 0008 / 0010 / 0011
    - `/docs/15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` — Phase 1 / 2 / 4 cross-references for Country Access Control
    - `/docs/CHANGELOG.md` — this entry
  - **Items deliberately NOT documented this turn (per scope guard):**
    - Full mobile admin country selector UI design (Phase 1 scaffold only; full UI Phase 6)
    - Time-limited grant admin UI (Phase 6)
    - Cross-country drift detection algorithm (Phase 10)
    - Grant-expiry automation worker (Phase 10)
    - Country-scope analytics dashboard (Phase 10)
- 2026-05-09: **Shipping & Logistics — configurable provider system locked. D-OPS-003 ANSWERED (KSA initial primary = J&T Express); D-OPS-010 ANSWERED (architectural lock on configurable providers + rate cards).**
  - **Hard rule (locked):** NO shipping company, tariff, delivery promise, COD fee, return fee, or shipping rule is hardcoded in application code. Adding a new courier OR a new rate card OR a new failed-delivery rule is a data operation in the admin dashboard, not a code change or a database schema change. CI grep test rejects hardcoded courier names / city lists / fees / SLA thresholds in any code path that evaluates shipping decisions.
  - **D-OPS-003 (KSA initial primary logistics partner):** **J&T Express** selected. Operational onboarding TODO list (12 sub-items: business account approval, rate card, COD support, RUH-01 pickup, label generation, tracking API/webhook, return workflow, failed delivery, reconciliation, city coverage, SLA by city, package size/weight limits) tracked in new doc `20-shipping-logistics/02-ksa-jt-express-onboarding.md`. Aramex KSA + SMSA + Bosta listed as fallback / future providers (Phase 6+ if J&T performance issues OR coverage gaps emerge). EG (Phase 8) and IQ (Phase 8) deferred — addable per the configurable architecture without code changes.
  - **D-OPS-010 (Configurable Shipping Providers + Rate Cards):** Architectural lock. Provider-agnostic, country-specific, dashboard-configurable shipping system.
    - **7 new tables reserved** as Phase 1 skeletons in Module 16 extension: `shipping_provider`, `shipping_method`, `shipping_rate_card`, `shipping_zone`, `shipping_rule`, `shipment`, `shipment_event`. Full column specs in `20-shipping-logistics/01-overview.md` §B.
    - **Money columns** use `numeric(12,2)` per D-DB-003.
    - **Class 1 soft-delete** per D-DB-010 (deleted_at / deleted_by / delete_reason or archived_at / archived_by / archive_reason).
    - **API credentials NEVER displayed in dashboard** — `shipping_provider.api_configured` boolean only; secrets in 1Password (per D-BKP-001) referenced via `op://vtc-prod-infra/shipping/<provider_code>` path.
    - **Manual mode** for providers without API integration; admin records label / tracking number manually.
    - **Audit log** on every provider / method / rate / rule change.
    - **Capabilities locked:** multiple providers per country; multiple methods per provider; multiple rate cards per (provider × method × city); switch primary provider via flag flip; fallback provider rule engine; per-country isolation.
  - **New admin section: Operations / Shipping** with 8 sub-pages (Shipping Providers, Shipping Methods, Shipping Rate Cards, Delivery Zones, Shipping Rules, Failed Delivery Rules, Return Shipping Rules, Shipping Provider Settings). Phase 1 = read-only stubs; Phase 2 = full editing UI.
  - **16 new permission slugs** added in 4 groups: `shipping_provider.*` (6 slugs: read/create/update/deactivate/archive/reactivate); `shipping_method.*` (4 slugs); `shipping_rate.*` (4 slugs); `shipping_rule.*` (2 slugs). Suggested role × permission matrix locked covering super_admin / operations_director / finance_admin / shipping_coordinator / admin / external_marketer.
  - **Country Launch Readiness extends** with 7 shipping readiness checks: `shipping.provider_active` / `shipping.method_active` / `shipping.rate_card_for_main_cities` / `shipping.delivery_promise_set` / `shipping.return_rule_exists` / `shipping.cod_fee_defined` / `shipping.coverage_documented`.
  - **Product / Landing Page Readiness extends** with 3 shipping checks: `product.delivery_promise_for_country` / `product.shipping_cost_calculable` / `product.customer_facing_delivery_message`.
  - **Phase placement:** Phase 1 = schema reservations + admin section structure + KSA J&T draft seed + audit log + 16 permission slugs. Phase 2 = checkout integration (fee calculation + delivery promise display + manual shipment mode + shipment row creation). Phase 4 = WhatsApp shipment status templates (5 templates pending Meta approval). Phase 7 = COD reconciliation against J&T daily/weekly settlement reports. Phase 9 = courier performance analytics + returns workflow + failed delivery recovery + J&T API integration (when API tier approved). Phase 10 = shipping intelligence (courier scorecards / route optimization / COD fraud patterns / dead-stock-by-courier-failure).
  - **Files created:** `/docs/20-shipping-logistics/01-overview.md` (15-section umbrella spec); `/docs/20-shipping-logistics/02-ksa-jt-express-onboarding.md` (Phase 0 weeks 1–3 + Phase 1 sprints 1–3 + Phase 2/4/7/9/10 deliverables checklist).
  - **Files updated:** `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (D-OPS-003 marked 🟢 Answered with J&T selection + operational TODOs; new D-OPS-010 added and 🟢 Answered; stats refreshed to 149 total / 28 answered / 12 open Phase 1 blockers / 21 answered Phase 1 blockers / 7 cross-phase architecture decisions answered; Top 10 reshuffled — D-LAUNCH-010 Phase 0 close target date promoted into rank #10); `/docs/01-database/02-tables-by-module.md` (Module 16 extended with shipping table list + cross-reference to `20-shipping-logistics/`); `/docs/03-rbac/02-permissions.md` (Shipping & Logistics permission group with 16 slugs + role × permission matrix); `/docs/11-admin-ui/01-information-architecture.md` (Operations / Shipping section structure + locked-workflow reference); `/docs/27-readiness-engine/01-unified-readiness.md` (Shipping readiness checks added: 7 country-level + 3 product-level); `/docs/15-phases/phase-1-acceptance.md` (Shipping & Logistics acceptance section with ~12 tests); `/docs/15-phases/phase-1-execution-plan.md` (Shipping schema reservation tasks + KSA J&T onboarding distribution); `/docs/15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (Phase 1 / 2 / 4 cross-references for shipping). Tracker stats refreshed.
- 2026-05-07: **D-DB-010 soft-delete vs hard-delete policy ANSWERED and locked: mixed policy with 4 classes.** Decision: business / legal importance varies by table; one-size-fits-all (soft everywhere or hard everywhere) is wrong; specific table-by-table classification is required.
  - **Class 1 — Soft-delete / archive only** (no hard delete during normal operations):
    - `customer_orders`, `customer`, `b2b_account`, `invoice`, `refund`, `payment_transaction`, `warranty_claim`, `service_ticket`, `product_batch`, `stock_movement`, `marketer_payout`, `approval`, `override_request`
    - Columns: `deleted_at timestamptz NULL` + `deleted_by uuid FK NULL` + `delete_reason text NULL`
    - Where archive lifecycle is more accurate (`product_batch`, `warehouse`, `service_ticket`): `archived_at` + `archived_by` + `archive_reason`
  - **Class 2 — Never hard delete** (immutable history):
    - `audit_log`, `country_data_audit`, `cost_history`, `variant_country_cost`, `variant_marketer_cost`, `order_profit_snapshot`, `payment_log`, `safety_incident_report`
    - **NO `deleted_at` column**; tables grow forever (or move to archive table per `19-performance-growth/...` §F.7)
    - CI grep test rejects `DELETE FROM <class-2-table>` in any migration or cleanup job
    - Only exception: `sensitive_cleanup_override` per `10-overrides/01-override-types.md` (Super Admin + Finance Admin dual + mandatory pre-cleanup backup)
  - **Class 3 — Hard delete allowed after retention policy**:
    - `cart`, `cart_line`, `cart_event`, expired sessions, reset tokens, temporary uploads, import preview files, expired payment links, expired stock reservations after release/logging, old raw pixel events after summarization, old webhook retry logs after retention/archive
    - Each has `retention_policy` row (per `19-performance-growth/...` §F.2) + `cleanup_job` row (Phase 1 reservation; cleanup execution Phase 10)
    - **No soft-delete columns** on these tables (would just bloat schema)
  - **Class 4 — PII anonymization (special case for `customer`)**:
    - Customer deletion requests follow PDPL right-to-erasure path
    - Financial records (linked `customer_orders`, `invoice`, `payment_transaction`) retained for legal reporting (Class 1)
    - `customer` row remains; PII fields (`name`, `phone`, `phone_normalized`, `email`, `customer_address.*`) replaced with `[ANONYMIZED]` markers
    - Anonymization audit-logged with `audit_log` row + `customer_consent` row referencing the right-to-erasure request
  - **Reporting consistency:** soft-deleted `customer_orders` REMAIN included in historical revenue reports unless explicitly filtered. Cancelled / refunded handled by `status`, NEVER by deletion. Dashboards show "Deleted" / "Archived" badge on rows where `deleted_at IS NOT NULL` or `archived_at IS NOT NULL`; default list filters exclude deleted; toggle reveals.
  - **Safety:** no hard delete for records linked to orders / payments / stock movements / warranty / safety / audit; destructive cleanup requires preview + backup if risky + approval if sensitive + audit log (per existing `sensitive_cleanup_override`).
  - **Updated:** `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (D-DB-010 marked 🟢 Answered with full 4-class policy; stats refreshed to 26 answered / 122 open / 13 open Phase 1 blockers / 20 answered Phase 1 blockers; Top 10 reshuffled — D-OPS-003 logistics partners promoted into rank #10 since the schema-shaping decision spine is now closed); `01-database/02-tables-by-module.md` (Conventions section: Deletion policy rule strengthened with full 4-class spec + per-class table list + Class 4 anonymization cross-reference); `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` (§F.2 retention rules: D-DB-010 callout + class × retention interaction matrix; §K rule 5 cross-references D-DB-010); `07-reporting/02-metric-definitions.md` (`gross_revenue` formula updated: deleted_at filter omitted by default; soft-deleted orders remain in historical totals; cancelled/refunded handled by status); `15-phases/phase-1-acceptance.md` (new "Deletion policy" section with 9 acceptance tests covering Class 1 columns / Class 2 no-deletion / Class 3 retention rows / Class 4 anonymization workflow / reporting consistency / cancelled-refunded by status / dashboard badges / CI grep / sensitive_cleanup_override gating); `15-phases/phase-1-execution-plan.md` (new "Deletion policy enforcement" task list distributed across migrations 0002/0007/0008/0010/0012). Tracker stats refreshed.
- 2026-05-07: **Strategic Enhancements approved and structured into /docs.** Five new module-class decisions answered + 4 new modules added + 5 cross-cutting infrastructure pieces locked. Scope strictly bounded: Phase 1 absorbs schema reservations + AI guardrail integration + documentation only; functional UI ships in later phases per the phase placement table.
  - **D-DEC-001 Decision & Recommendation Engine** (🟢 Answered): Module 24 unifies Ladder Finder + AI recommendations + WhatsApp sales + checkout warnings under one rule engine. Three responsibilities: Recommend (best SKU + 1–3 alternatives + reasoning), Score (Suitability Score 0–100 per (variant, use_case)), Warn ("Don't Sell Wrong Ladder" warning layer with hard constraints). New AI tool `recommendation.evaluate`. New override type `recommendation_override`. Phase 1 schema column reservation (`product_variant.suitability_scores jsonb`); Phase 6 build.
  - **D-SAFE-001 Safety & Compliance Center** (🟢 Answered, 🛑 Phase 1 blocker — required before safety claims ship): Module 25 with safety_guideline / safety_claim / safety_claim_approval / safety_incident_report tables + `country.compliance_profile jsonb` column. Forbidden claims list locked: "100% accident-proof", "lifetime guarantee", "best in market" without proof, "certified" without specific cert ID, competitor-name comparisons, medical claims. AI cannot mention any safety claim without approved row + jurisdiction match. Pre-publish landing page compliance check consults `safety_claim` rows. Safety incident reports auto-create Critical alerts; 10-year retention; recall procedure with 3-party approval. New override type `safety_claim_override`. Phase 1 schema reservation + AI guardrail integration ACTIVE; Phase 6 admin UI.
  - **D-TRUST-001 Trust Layer** (🟢 Answered): Module 26 with `customer_review` / `customer_review_media` / `customer_review_approval_log` tables + verified-purchase view. Phase 1 schema reservation; Phase 4 collection via WhatsApp post-delivery; Phase 6 surface rendering on PDP / PLP / Landing pages. Customer photo permission captured explicitly via WhatsApp acknowledge / web checkbox / email reply; revocation removes from public surfaces within 1 cache cycle. AI moderation auto-flags safety mentions → routes to Module 25. 1-2 star reviews always trigger Customer Support service-recovery review.
  - **D-READY-001 Unified Readiness Engine** (🟢 Answered): Module 27 unifies country_launch_readiness + product_country_readiness + landing_page_quality_gate_run into one model (`readiness_check_definition`, `readiness_check_run`, `readiness_blocker`, `deploy_log`). Phase 1 schema reservation; Phase 6 refactor; Phase 10 unified dashboard. Launch gate logic: critical checks pass; high checks pass or override; medium/low surface but don't block.
  - **D-OWN-001 Data Ownership Matrix** (🟢 Answered, 🛑 Phase 1 blocker — required before role_permission migration 0002): CSV at `03-rbac/matrix/data-ownership-matrix.csv` v1 with ~50 entity rows. Powers RBAC + Alerts Center routing + SOPs + Smart Notifications. Companion to D-PERF-004 role × permission CSV.
  - **Files created (16):**
    - `/docs/24-decision-engine/01-overview.md`
    - `/docs/25-safety-compliance/01-overview.md`
    - `/docs/25-safety-compliance/02-safety-claim-policy.md`
    - `/docs/25-safety-compliance/03-incident-response.md`
    - `/docs/25-safety-compliance/04-certificates-policy.md`
    - `/docs/26-trust-layer/01-overview.md`
    - `/docs/27-readiness-engine/01-unified-readiness.md`
    - `/docs/15-phases/promotion-checklist.md`
    - `/docs/03-rbac/matrix/data-ownership-matrix.csv`
    - `/docs/03-rbac/matrix/README.md`
    - `/docs/18-runbooks/wrong-ladder-customer-complaint.md` (stub)
    - `/docs/18-runbooks/safety-incident-response.md` (stub)
    - `/docs/18-runbooks/batch-recall-procedure.md` (stub)
    - `/docs/18-runbooks/landed-cost-spike-investigation.md` (stub)
    - `/docs/18-runbooks/recommendation-engine-disagreement.md` (stub)
    - `/docs/18-runbooks/change-request-stuck-in-approval.md` (stub)
  - **Files updated (11):** `/docs/01-database/02-tables-by-module.md` (Modules 24/25/26/27 schema reservations + cross-cutting columns + cost_read_log table); `/docs/03-rbac/02-permissions.md` (~30 new permission slugs across Decision Engine / Safety / Trust / Warranty / Batch Quality / Readiness / Cost-Read Audit groups); `/docs/04-ai/05-guardrails.md` (Wrong-Ladder Warning Rules + Safety Claim Restrictions + extended output validator); `/docs/10-overrides/01-override-types.md` (added `safety_claim_override` + `recommendation_override` — total now 15 override types); `/docs/11-admin-ui/01-information-architecture.md` (added Decision & Recommendation, Safety & Compliance, Trust & Reviews, Readiness Engine sidebar groups + 4 new sub-page structures + locked-workflow references); `/docs/15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (5 new D-* decisions added; stats refreshed to 148 total / 25 answered / 14 open Phase 1 blockers / 19 answered Phase 1 blockers / 6 cross-phase architecture decisions answered); `/docs/15-phases/phase-1-acceptance.md` (new "Strategic enhancements — schema reservations" section with ~20 acceptance tests covering schema reservations, AI guardrail integration, forbidden claims grep test, no-hardcoded-safety-claim grep, Data Ownership Matrix exists, Promotion Checklist exists, Staging Safety Rules documented, cost_read_log table reserved, cross-cutting columns reserved); `/docs/15-phases/phase-1-execution-plan.md` (new Strategic Enhancements section listing schema-reservation tasks distributed across migrations 0002/0004/0005/0007/0010/0012 + explicit Phase 1 NOT-expanded scope list); `/docs/15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (Phase 1 / 4 / 6 / 10 cross-references for strategic enhancements); `/docs/00-architecture/environment-topology.md` (new "Staging Safety Rules" section with 15 enforced rules covering noindex / robots disallow / Basic Auth / no-prod-credentials / no-prod-WhatsApp / no-prod-AI / no-real-customer-data / no-prod-pixels / no-prod-webhooks / staging watermark / vault references / restore-test isolation / email sink / SMS suppression).
  - **New permission slugs (~30 added):** Decision Engine (3); Safety & Compliance (12); Trust Layer (6); Warranty Registration (2); Batch Quality (2); Readiness Engine (4); Cost-Read Audit Logging (1).
  - **New override types (2):** `safety_claim_override` (Super Admin + Compliance Officer dual; 30d expiry); `recommendation_override` (Super Admin only; 14d expiry). Total override types now 15.
  - **Deliberately deferred (per scope-creep guard):**
    - Full Supplier & Purchase Planning module — defer entirely (ERP creep)
    - Separate Ladder Academy module — kept as content tag within Module 13
    - Separate Change Request module — uses existing approval + override systems
    - Full Offer Intelligence ML algorithm — Phase 10
    - Full True Net Profit formula — Phase 10
    - Full Warranty Registration UI screens — Phase 9
    - Customer Risk Score scoring formula — Phase 7+
    - AI Conversation Drift Detection algorithm — Phase 10
    - Landed Cost full breakdown — Phase 8 schema-reservation only this turn
    - Detailed runbook procedures (stubs only this turn)
- 2026-05-07: **D-DB-003 numeric precision per currency ANSWERED and locked: `numeric(12,2)` for ALL monetary database columns.** Decision: keeps schema homogeneous across KSA / EG / IQ; supports SAR + EGP at 2 decimals natively; supports IQD even when displayed/rounded to 0 decimals (DB stores full precision); avoids per-country schema differences; avoids precision loss; simplifies reporting, payments, refunds, payouts, invoices, marketer profit, and order snapshots.
  - **Locked rules:**
    - **All monetary columns** use `numeric(12,2)` — uniformly across SAR / EGP / IQD. Affected tables: `variant_country_price`, `variant_country_cost`, `variant_marketer_cost`, `customer_orders.totals` jsonb, `order_line` (`unit_selling_price`, all `*_snapshot` + `*_share`), `payment_transaction.amount`, `refund.amount`, `deposit.amount`, `invoice.total`, `marketer_payout.amount`, `order_profit_snapshot.*`, `campaign_profit_forecast.*`, all reporting MVs.
    - **Margin / rate / percentage columns** use `numeric(5,4)` (distinct from money) — e.g., `country.business_min_margin`, `vat_rate`, `platform_fee_percent_snapshot`.
    - **Display vs storage separation:** DB stores full `numeric(12,2)` precision regardless of currency; display rounding happens at presentation layer via `country.price_rounding_pattern` + `currency.decimals` (IQD displays at 0 decimals; SAR/EGP at 2). Reports compute in full precision; cells render with rounding applied last.
    - **Forbidden types:** `float`, `double precision`, `real`, `money` (Postgres type), or any `numeric` with non-`(12,2)` precision on a monetary column; per-country numeric precision variation; storing minor units (halalas/piasters/cents) at the DB layer.
    - **Payment minor-unit conversion** (e.g., Stripe halalas = SAR × 100; piasters = EGP × 100; IQD has no minor unit) handled in **payment adapter layer only** — never at DB. Round-half-up at conversion boundary. Banker's rounding rejected for retail consistency. Round-trip safety: `from_provider(to_provider(x)) === x` must hold.
    - **Snapshot rule:** every monetary value referenced on a `customer_orders` line is snapshotted into `order_line.*_snapshot` columns at order creation; never recomputed.
    - **No floating-point arithmetic at any layer** — adapter code uses integer math (`amount × 100` after parsing as `Decimal`/`bigint`) — never `parseFloat` or `Number()` on monetary strings.
    - **Currency code travels with the amount** — every adapter call includes the currency; never assume a default. Mismatched currency between order and provider call → reject.
    - **CI grep test** rejects any migration containing forbidden monetary types (`float`/`double precision`/`real`/`money`/non-`(12,2)` numeric on monetary columns); allow-list permits `numeric(5,4)` for rate/percentage columns + `numeric(*,*)` on non-monetary fields.
  - **Updated:** `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (D-DB-003 marked 🟢 Answered with full lock notes; stats refreshed to 20 answered / 123 open / 14 open Phase 1 blockers / 17 answered Phase 1 blockers; Top 10 reshuffled — D-DB-010 soft-delete vs hard-delete policy promoted into rank #10), `01-database/02-tables-by-module.md` (Conventions section: Money rule added — `numeric(12,2)` mandatory for all monetary columns + display vs storage separation + forbidden-type list + snapshot rule + payment-adapter pointer; new Margin/rate rule clarifying `numeric(5,4)` for non-money rates; TODO marked Answered), `01-database/05-migration-plan.md` (locked-rules table extended with money-precision rule; CI fail rule extended with money-precision pattern; new TODO for grep step including allow-list for `numeric(5,4)`), `05-payments/02-provider-abstraction.md` (new "Money & Minor Units (🟢 D-DB-003 locked 2026-05-07)" section: conversion rules table per currency × provider, 8 hard rules covering DB precision / adapter responsibility / round-half-up / IQD whole-unit rule / round-trip idempotency / boundary-value test coverage / no-floating-point / currency-code-travels-with-amount; "Currency display" subsection separating storage from presentation; 3 new TODO clarifications), `05-payments/14-profit-floors-and-guardrails.md` (D-DB-003 callout at top distinguishing money columns `numeric(12,2)` from rate columns `numeric(5,4)`), `07-reporting/02-metric-definitions.md` (D-DB-003 callout at top: monetary metrics computed in `numeric(12,2)`; display rounding at presentation layer never inside SQL; cross-country reports require `currency_exchange_rate` Phase 8+), `15-phases/phase-1-execution-plan.md` (scope item #5 references D-DB-003 + `numeric(12,2)` + adapter-layer minor-unit conversion + CI grep test), `15-phases/phase-1-acceptance.md` (new "Monetary precision (🟢 D-DB-003 locked 2026-05-07; `numeric(12,2)` for all money columns)" section with 8 acceptance tests covering: all monetary columns declared `numeric(12,2)` / no forbidden types / no per-country variation / IQD precision test / order snapshot immutability / payment adapter round-trip test / no floating-point in adapter / margin columns use `numeric(5,4)` / CI grep rejection). Tracker stats refreshed.
- 2026-05-07: **D-DB-002 UUID generation strategy ANSWERED and locked: `gen_random_uuid()` from `pgcrypto` for ALL UUID primary keys.** Decision: modern PostgreSQL-native approach; avoids dependency on `uuid-ossp`; simple default expression `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`; works well with `node-pg-migrate` (D-INFRA-004); keeps UUID strategy consistent across the schema.
  - **Locked rules:**
    - Migration 0001 must enable `pgcrypto` via `CREATE EXTENSION IF NOT EXISTS pgcrypto`.
    - **ALL** UUID primary keys default to `gen_random_uuid()` — no exceptions.
    - **`uuid_generate_v4()` is forbidden** anywhere in migrations or app code.
    - **`uuid-ossp` extension is forbidden** — must NOT be enabled.
    - **Mixing UUID generation strategies is forbidden** — no per-table custom UUID generators.
    - CI grep test rejects any migration file or app-code file containing `uuid_generate_v4` or `CREATE EXTENSION ... uuid-ossp` (allow-list: comments, prose).
  - **Updated:** `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (D-DB-002 marked 🟢 Answered with full lock notes; stats refreshed to 19 answered / 124 open / 15 open Phase 1 blockers / 16 answered Phase 1 blockers; Top 10 reshuffled — D-DB-003 numeric precision per currency promoted into rank #10), `01-database/02-tables-by-module.md` (Conventions section: PK rule strengthened — "ALL UUID primary keys" with explicit forbidden-tools list + CI grep note; TODO entries for D-DB-001 + D-DB-002 marked Answered), `01-database/05-migration-plan.md` (Migration 0001 spec gets D-DB-002 lock callout + standard PK column definition snippet + explicit `-- D-DB-002` annotations on the SQL; locked-rules table extended with UUID rule; CI fail rule extended; new TODO for grep step), `00-architecture/adr/ADR-006-database-engine.md` (`pgcrypto` extension annotated with D-DB-002 lock and forbidden uuid-ossp note), `00-architecture/adr/ADR-027-migration-tool.md` (CI fail rule list extended with uuid_generate_v4/uuid-ossp grep rejection), `15-phases/phase-1-execution-plan.md` (scope item #5 references D-DB-002 + `gen_random_uuid()` + CI grep test), `15-phases/phase-1-acceptance.md` (new "UUID generation strategy (🟢 D-DB-002 locked 2026-05-07; `gen_random_uuid()` from pgcrypto)" section with 7 acceptance tests covering: pgcrypto enabled / uuid-ossp NOT enabled / all UUID PKs default to gen_random_uuid / CI grep rejects uuid_generate_v4 / CI grep rejects uuid-ossp / no mixed generators / PR template UUID-strategy checkbox). Tracker stats refreshed.
- 2026-05-07: **D-DB-001 `order` reserved-word collision ANSWERED and locked: rename to `customer_orders` (plural).** Decision: avoid SQL reserved-word confusion; cleaner queries; no quoting required; lower risk in migrations / reports / joins / APIs / future integrations; better naming clarity for ecommerce context. **Naming convention locked:**
  - **Main table = `customer_orders`** (plural).
  - **Child tables retain `order_*` prefix where already standardized** — `order_line`, `order_event`, `order_address`, `order_profit_snapshot`, `order_source_attribution`, `order_risk_score` — because `order_X` does not collide with the SQL `ORDER BY` keyword (only the bare token `order` does).
  - **Future child tables** follow the same rule: `customer_order_payments`, `customer_order_addresses` etc. only if no equivalent `order_*` name already exists; preserve existing standardized child names.
  - **FK column names** stay as `order_id` (also non-colliding; massive churn to rename across audit/cart/marketer-attribution/payments/delivery tables for marginal clarity benefit).
  - **Webhook event names** stay as `order.created`, `order.confirmed`, `order.cancelled`, `order.delivered`, `order.refunded` — external API contract; entity-action dot syntax never runs as SQL. The entity backing these events is `customer_orders`.
  - **View name `v_order_line_public`** preserved (no SQL collision); underlying parent table is `customer_orders`.
  - **Index name `idx_order_*`** preserved (identifiers are not the bare reserved word).
  - **Forbidden:** bare `order` table name in any new schema; quoted `"order"` references in app code, queries, or migrations. CI grep test rejects either pattern; rule documented in `01-database/05-migration-plan.md` Locked rules table + ADR-027.
  
  Updated: `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (D-DB-001 marked 🟢 Answered with full naming convention; stats refreshed to 18 answered / 125 open / 16 open Phase 1 blockers / 15 answered Phase 1 blockers; Top 10 reshuffled — D-DB-002 UUID generation strategy promoted into rank #10), `01-database/02-tables-by-module.md` (Module 6 renamed `### \`order\`` → `### \`customer_orders\`` + naming-convention callout block at module top + FK note on `order_line.order_id`), `01-database/01-erd.md` (Cluster 2 + Cluster 4 + Cluster 7 mermaid diagrams updated to `customer_orders`; cluster heading + callout note added), `01-database/03-rls-policies.md` (`order_line` cost columns section gets D-DB-001 callout note explaining parent rename + child name preservation), `01-database/04-indexes.md` (Orders section `"order"` quoted identifiers replaced with bare `customer_orders`; D-DB-001 callout note), `01-database/06-views.md` (`v_order_line_public` callout note explaining parent rename + view name preservation), `01-database/05-migration-plan.md` (Migration 0008 spec renamed + locked-rules row added forbidding bare `order` and quoted `"order"`; CI fail rule added; new TODO for grep step), `02-api/03-webhook-events.md` (Order section gets D-DB-001 callout — webhook event names preserved; entity backing is `customer_orders`), `07-reporting/02-metric-definitions.md` (gross_revenue / gross_profit / aov formulas updated to `customer_orders`), `00-architecture/adr/ADR-027-migration-tool.md` (Context section gets D-DB-001 companion lock note + CI fail rule expanded), `15-phases/phase-1-execution-plan.md` (scope item #5 references D-DB-001 + `customer_orders`), `15-phases/phase-1-acceptance.md` (new "Schema naming convention (🟢 D-DB-001 locked 2026-05-07; `customer_orders`)" section with 6 acceptance tests covering: `customer_orders` exists / no bare `order` table / no quoted `"order"` references / child tables retain prefix / FK references verified / webhook event names preserved). Tracker stats refreshed.
- 2026-05-07: **D-PERF-001 follow-up documentation TODOs completed — schema reservations, override types, Phase 1 acceptance, role-permission matrix planning, Script Inventory joint review owner.** Six work-streams completed in a single update:
  - **(1) Database schema reservations.** New `01-database/02-tables-by-module.md` **Module 23 — Performance, Cleanup, Landing Intelligence & Alerts** added with 12 planning-stage table reservations, each with full column spec + phase placement: `feature_flag` (Phase 1 skeleton; UI Phase 6+), `script_inventory` (Phase 6 — with **`security_reviewer_user_id` + `business_owner_user_id` both NOT NULL** for joint review per D-PERF-002), `landing_page_performance_history` (Phase 10), `cleanup_job` + `cleanup_job_run` (Phase 1 skeleton; execution Phase 10), `retention_policy` (Phase 1 with 13 seed rows; admin UI Phase 10), `alert` (Phase 1 read-only skeleton with Performance/Database/Backup categories; full categories Phase 6/10), `alert_subscription` + `notification_rule` (Phase 10), `media_quality_issue` (Phase 10 — Auto Image Quality Scanner output), `landing_page_quality_gate_run` + `landing_page_compliance_rule` (Phase 6). Reservation summary table added with cross-references to Modules 13/14/19/20/22.
  - **(2) Override types.** `10-overrides/01-override-types.md` extended from 8 to 13 types. Five new override types added (per D-PERF-003 🟢 Answered): `performance_budget_override` (Super Admin + Frontend Lead, dual, 14d expiry; mandatory remediation_plan), `landing_quality_gate_override` (Super Admin OR Marketing Manager; dual when Critical-fail count >1; 7d; cannot bypass missing-price/stock/CTA/coupon/compliance Critical blocks — those have their own overrides), `landing_profit_guardrail_override` (Super Admin OR Finance Admin; 7d; mandatory money-terms justification), `landing_inventory_guardrail_override` (Super Admin + Marketing Manager, dual, 72h; mandatory out-of-stock CTA mode), `sensitive_cleanup_override` (Super Admin + Finance Admin, dual, max 30d; **pre-cleanup auto-backup mandatory and cannot be skipped**; mandatory legal_compliance_basis). All 5 require **reason ≥30 chars** (vs 10 chars baseline), audit log, frozen context_snapshot, server-side auto-revert on expiry, webhook on issue/expiry/revoke, Smart Notification at 50% expiry window. Hard rules section + cross-reference table + 3 new TODO clarifications added.
  - **(3) Phase 1 acceptance criteria.** `15-phases/phase-1-acceptance.md` Performance section expanded with 7 new test categories: **Performance budget enforcement** (CI gate per page class; runtime RUM alerts; override path documented), **Image variant pipeline** (6 variants × AVIF/WebP/JPG ladder; Sharp build-time + Cloudflare runtime; alt_translations CI grep), **Heavy-asset rules** (no autoplay video; poster image required; preload="none"; 2MB ceiling), **AI chat lazy-load rule** (Phase 1 documents; Phase 3 enforces via lint), **Script weight inventory placeholder** (table reserved + 4 seed rows + read-only admin stub + CSP enforced from Day 1 + joint Security+Marketing reviewer documented), **Basic cache policy** (CDN page-class profiles; country-aware keys verified by 4th-country test; never-cache list; PDP ISR 15-min revalidate; smart invalidation; static snapshot policy), **Alerts Center skeleton** (`alert` table reserved; read-only admin page; Performance/Database/Backup insertion paths wired; failed-backup Critical SLA notification test), **Cleanup framework documentation** (`retention_policy` 13 seed rows + `cleanup_job` skeletons + read-only admin route).
  - **(4) Role-permission matrix planning.** `03-rbac/02-permissions.md` "Machine-readable matrix planning" section locked (per D-PERF-004 🟢 Answered): CSV at `03-rbac/matrix/role-permission-matrix.csv` covering **24 roles × ~95 permissions (~2,280 cells)** required before Phase 1 sprint 2 (RLS + role seeding migration 0002). Cell values locked: `Y / N / scoped / dual_approval / read / own / view_alerts / partial`. CSV powers: (a) migration 0002 `role_permission` seed generator, (b) CI permission test asserting CSV ↔ DB consistency, (c) admin Permissions Inspector at `/admin/system/users/permissions-inspector`. CSV wins over Markdown from Phase 1 sprint 2 onwards. PR template will require CSV row + role assignment review for any new permission slug. New permissions from D-PERF-001 enumerated (~25 across Performance/Landing Pages/Data Maintenance/Alerts/Database Health). **5 new override-approval slugs added** (`override.approve.performance_budget`, `.landing_quality_gate`, `.landing_profit_guardrail`, `.landing_inventory_guardrail`, `.sensitive_cleanup`). Two new role columns confirmed: `infra_lead`, `dba`.
  - **(5) Script Inventory review owner locked (D-PERF-002 🟢 Answered): Joint Security Lead + Marketing Manager.** Security Lead approves risk/security (CSP impact, sandboxing, supply-chain risk, exfiltration vector); Marketing Manager owns business justification + activation need (campaign attribution, ROAS, audience pixels, business value vs perf cost). Approval requires both. Either reviewer can pause unilaterally; reactivation requires both again. Custom scripts add Super Admin as 3rd sign-off. Schema enforces via `script_inventory.security_reviewer_user_id` + `business_owner_user_id` both NOT NULL. Documented in `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` §A.6 with new "Joint review ownership" subsection.
  - **(6) Tracker updates.** `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` Section 16 expanded with **D-PERF-002**, **D-PERF-003**, **D-PERF-004** (all 🟢 Answered 2026-05-07). Stats refreshed: **143 total decisions / 17 answered / 17 open Phase 1 blockers / 14 answered Phase 1 blockers / 3 cross-phase architecture decisions answered**.
  - **Files updated:** `01-database/02-tables-by-module.md` (Module 23 added), `10-overrides/01-override-types.md` (5 new override types + required-fields + server-enforcement + TODOs), `15-phases/phase-1-acceptance.md` (7 new acceptance test categories), `03-rbac/02-permissions.md` (Machine-readable matrix planning section + new TODOs), `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` (§A.6 Joint review ownership subsection + Hard rules refresh), `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (Section 16 expansion + stats refresh).
- 2026-05-07: **D-PERF-001 Performance, Cleanup, Landing Page Growth & Alerts System ANSWERED and locked.** New cross-cutting platform module documented at `19-performance-growth/PERFORMANCE_CLEANUP_LANDING_ALERTS_SYSTEM.md` (🟢 Confirmed and locked 2026-05-07). Spans Phase 1 → Phase 10. **Sections (A–L):** A. Performance Engineering (CDN strategy, country-aware caching, page-class profiles, performance budgets enforced via CI: 1.0–1.8 MB / Lighthouse 90–95+; CWV targets LCP <2s / INP <200ms / CLS <0.05; JavaScript control rules; Script Weight Governance with 9-field `script_inventory` + quarterly review). B. Image/Video Optimization (6 required variants per asset: Hero Desktop 1920×1080, Hero Mobile 768×1024, Product Card 600×600, Thumbnail 200×200, OpenGraph 1200×630, WhatsApp preview 800×800; AVIF/WebP/JPG fallback ladder; Auto Image Quality Scanner Phase 10). C. Landing Page Growth System (product LPs with 17-field `landing_page` schema + 15-section reusable template; 8 category LPs; Landing Page Builder admin module under new **Growth & Performance** sidebar group). D. Advanced Landing Page Intelligence (Pre-Publish Quality Gate with 16 checks; Landing Page Score 0–100; Product Page Readiness Score; 14-block library; AI Draft Assistant always `pending_review`; Compliance Check forbidden-claims list; Profit Guardrail integrated with `05-payments/14-profit-floors-and-guardrails.md`; Inventory Guardrail with low-stock warnings + alternative product routing; Adaptive Landing Pages by country/city/source/device/visitor; Smart CTA Engine 6 contexts; Campaign-Specific Variants; Objection-Based + Buyer Persona + City+Product programmatic SEO pages). E. Cache Strategy (6 rules: country-aware keys; never-cache list; static landing snapshots with 30-day rollback retention; SWR; tag-based smart invalidation — never whole-site purge). F. Data Maintenance & Cleanup System (13-row retention table: orders/invoices/audit/payment 7y; messages 2–3y; AI logs 12–24mo; webhooks/pixels 90–180d; sessions 24h–30d; 14 scheduled cleanup jobs; Safe Cleanup Workflow: preview → risk classification → backup → approval → execute → audit → report; 6 lifecycle states; Cleanup Preview Report with sample rows; Database Archiving Layer for append-only logs). G. Database Health & Report Optimization (DB Health Dashboard with slow queries / index health / bloat / VACUUM status; 8 partitioning candidates at 50M-rows / 50GB threshold; 8 summary tables refreshed hourly/daily; 3 new Phase 10 materialized views: `mv_landing_page_performance`, `mv_script_weight_impact`, `mv_cleanup_history`). H. Monitoring/Incidents/Alerts System (new **System / Intelligence → Alerts Center** branch; 8 alert categories: Performance/Landing/Database/Cleanup/Payment/WhatsApp+Messaging/AI/Business; 5 severity levels: info/low/medium/high/critical; 18-field `alert` schema; alert workflow triggered → assigned → acknowledged → investigating → resolved → closed; Critical SLA 15min default; Performance Incident System with auto-incidents; Page Performance History; Revenue Impact of Slowness reports; Slow Query Business Impact). I. Feature Flags (16 suggested flags; 7-attribute schema: country/locale/role/percentage/start_at/end_at/rollback/audit_log; `feature_flag` table reserved Phase 10). J. Permissions (~25 new slugs across 5 groups: Performance/Landing Pages/Data Maintenance/Alerts/Database Health + role × permission matrix). K. 18 hard Safety Rules (locked: no destructive cleanup without preview; backup required for risky cleanup; sensitive cleanup requires approval; PII compliance per `17-compliance/`; orders/payments/audit/invoices retained 7y minimum; AI-generated copy requires human approval; landing pages cannot publish without active price + stock unless waitlist; performance regression auto-creates alerts; static snapshots retain 30-day rollback). L. Cross-references to 9 coordinated docs. **Phase 1 ships:** performance budgets enforcement (CI gate), CWV monitoring, image variants pipeline, basic alerts skeleton, retention rule documentation, cleanup framework. **Phase 6 ships:** landing builder + Growth & Performance admin module, A/B tests, AI draft assistant, Smart CTA, adaptive pages, static snapshots, script inventory. **Phase 10 ships:** Auto Image Quality Scanner, partitioning, summary tables, full cleanup automation, performance incident automation, revenue-impact reports. Updated: `03-rbac/02-permissions.md` (new permission catalog + role × permission matrix for Performance/Landing/Cleanup/Alerts/DB Health), `11-admin-ui/01-information-architecture.md` (added **Growth & Performance** sidebar group with 14 sub-pages + **System / Intelligence → Alerts Center** branch with 12 sub-pages + **Data Maintenance** + **Database Health** sub-sections + locked-workflows reference), `07-reporting/03-data-quality-issues.md` (landing-page-quality issue types extended), `15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (Phase 1/6/10 cross-references), `15-phases/PHASE_0_DECISIONS_TODO_TRACKER.md` (new section 16 with D-PERF-001 marked 🟢 Answered). Tracker stats: **140 total, 14 answered, 17 open Phase 1 blockers** (Phase 1 blockers count unchanged because D-PERF-001 spans phases as a cross-phase architecture decision, not a strict Phase 1 gate).
- 2026-05-07: **Domain root confirmed** — primary website domain is `ladders.vtc-me.com` (replaces `smartladders.example` placeholder across `00-architecture/environment-topology.md`, `02-api/02-openapi.yaml`, `12-public-site/01-sitemap.md`). `tl.vtc-me.com` reserved as a possible future short tracking/redirect domain for marketer links (Phase 5–6+ decision; not active in Phase 0/1).
- 2026-05-07: **Product Add/Edit dynamic Country Pricing & Warehouse Stock requirement CONFIRMED and locked.** New doc `11-admin-ui/06-product-add-edit-workflow.md` specifies the matrix UI (per-country pricing/availability tab + per-warehouse stock tab). Schema callouts added to `01-database/02-tables-by-module.md` Modules 3 (Pricing & Costs) and 4 (Stock & Warehouses). Phase 1 acceptance criteria expanded with explicit matrix tests. Information Architecture and Screen List point to the new workflow doc. Tracker decision **D-COUNTRY-014** marked 🟢 Answered. Added `delivery_promise_note text NULL` field to `variant_country_price`. Hardcoded country/warehouse fields (`price_sa`, `stock_eg`, etc.) explicitly **forbidden**. Tracker stats: 138 total decisions, 2 answered (D-INFRA-018 domain + D-COUNTRY-014 dynamic matrix), 27 open Phase 1 blockers.
- 2026-05-07: **Launch catalog CONFIRMED — 10 SKUs across all 3 launch countries (KSA, EG, IQ).** New doc `15-phases/LAUNCH_CATALOG.md` lists: VTC-TEL-OS-3.2M / -3.8M / -4.4M / -5.1M / VTC-TEL-DS-3.8M / -4.4M / VTC-MP-3.6M / -4.7M / VTC-HOME-4STEP / -5STEP. Hero SKU for Phase 1 = `VTC-TEL-OS-4.4M`. Per-country pricing, stock, costs, payments, WhatsApp number, delivery promise, tax rules remain country-specific. Tracker decisions **D-LAUNCH-009** and **D-COUNTRY-001** marked 🟢 Answered. Top 10 priority list reshuffled — D-COUNTRY-002 promoted into Top 10. Project Timeline doc (§4 Phase 1, §16 Critical Dependencies, §18 Immediate Next Actions), Photography Brief (`13-brand/photography-brief.md`), and Phase 1 Execution Plan updated to reference the locked catalog. Tracker stats: 138 total, **4 answered**, **25 open Phase 1 blockers**.
- 2026-05-07: **Launch warehouse plan CONFIRMED.** Phase 1 active warehouse = **Riyadh Main Warehouse (`RUH-01`)**. Planned for later phases: Jeddah Secondary Warehouse (`JED-01`) for KSA expansion, Cairo Main Warehouse (`CAI-01`) for Phase 8 Egypt launch, Baghdad Main Warehouse (`BGD-01`) for Phase 8 Iraq launch. Architecture remains dynamic — every warehouse linked to `country_id` + `city`; stock lives in `variant_warehouse_stock(variant_id, warehouse_id)`. Tracker decision **D-COUNTRY-002** marked 🟢 Answered. Top 10 reshuffled — D-INFRA-004 (migration tool) promoted into rank #10. Updated: `LAUNCH_CATALOG.md` §6 (full warehouse plan + Phase 1 seed spec), `PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (§16 Warehouse Cities row, §18 Immediate Next Actions priority 10 → DONE), `phase-1-execution-plan.md` (RUH-01 referenced explicitly + migration 0012 seed updated to include 1 warehouse row + 10 variant_warehouse_stock rows). Tracker stats: 138 total, **5 answered**, **24 open Phase 1 blockers**.
- 2026-05-07: **Warehouse Management workflow CONFIRMED and locked.** New doc `11-admin-ui/07-warehouse-management.md` specifies the full lifecycle: Add → Edit → Deactivate → Archive → Reactivate → Hard-delete, with server-side stock-safety check before deactivate/archive (blocks if any `qty_on_hand−qty_reserved > 0`, `qty_reserved > 0`, pending stock_transfer, or unfulfilled order references the warehouse). Hard-delete is super_admin only with reason ≥30 chars + audit snapshot. **8 fine-grained permission slugs** introduced (`warehouse.read/create/update/deactivate/archive/reactivate/delete_empty/transfer_stock`); deprecated `warehouse.write`. Schema delta added to `warehouse` table: `contact_person`, `phone`, `notes`, `archived_at`, `archived_by`, `archive_reason`. New tracker decision **D-COUNTRY-015** added and immediately marked 🟢 Answered. Updated: `01-database/02-tables-by-module.md` Module 4 (warehouse fields + lifecycle states), `03-rbac/02-permissions.md` (slugs + role × permission matrix), `11-admin-ui/01-information-architecture.md` (locked-workflows reference), `11-admin-ui/02-screen-list.md` (5 warehouse-related screens with phase placement), `15-phases/phase-1-acceptance.md` (14 new warehouse-management acceptance tests). Tracker stats: **139 total, 6 answered, 24 open Phase 1 blockers**.
- 2026-05-07: **D-RBAC-001 cost.read role gate ANSWERED and locked.** Only `super_admin` and `finance_admin` may read `actual_product_cost`. **18+ other roles excluded** (admin, country_manager, product_manager, inventory_manager, warehouse_manager, warehouse_staff, marketing_manager, marketer_manager, sales_manager, sales_agent, customer_support_agent, ai_supervisor, content_seo_editor, media_manager, b2b_sales_agent, maintenance_service_agent, shipping_coordinator, read_only_auditor, developer_api_admin, external_marketer). Marketer cost is governed by separate permission `marketer_cost.read` (Marketing/Marketer Manager + own marketer rows). Defense in depth: RLS at DB · serializer-strip at API · admin UI hide · `cost.export` scope for exports · webhooks redact value · audit log entries redacted for non-finance reads · Sentry/Grafana scrubs cost field names. Updated: `03-rbac/04-cost-privacy.md` (locked-decision callout with explicit excluded-roles list), `03-rbac/01-roles.md` (refreshed cost-access summary table), `11-admin-ui/06-product-add-edit-workflow.md` (reinforced RLS gates on Country Pricing cost row), `15-phases/phase-1-acceptance.md` (RLS test matrix expanded to test EVERY non-finance role + positive tests for super_admin/finance_admin + cost.export scope test + webhook redaction test + Sentry scrubber test + CI grep test). Tracker decision **D-RBAC-001** marked 🟢 Answered. Top 10 reshuffled — **D-PAY-010 (payment provider applications submitted Day 1) promoted into rank #10** due to long-lead approval cycles. Tracker stats: **139 total, 7 answered, 23 open Phase 1 blockers**.
- 2026-05-07: **D-PAY-002 + D-PAY-003 profit margin floors ANSWERED and locked.** Per-country business floors: **KSA 20% (`0.2000`) · Egypt 18% (`0.1800`) · Iraq 22% (`0.2200`)**; uniform marketer floor **5% (`0.0500`)** across all launch countries. New doc `05-payments/14-profit-floors-and-guardrails.md` specifies storage (`country.business_min_margin` cache + `profit_floor_rule` table with 6 seed rows), Phase 1 soft-warning behavior, Phase 5 hard-block + override flow, marketer-cost-vs-actual-cost separation, and admin adjustment workflow. Override below floors requires Manual Override with Reason (≥30 chars) + Audit Log per `10-overrides/01-override-types.md` `profit_guardrail` type. Marketer profit calculations MUST use `marketer_product_cost`, NEVER `actual_product_cost` (locked rule per D-RBAC-001 + D-PAY). Updated: `01-database/02-tables-by-module.md` (`country.business_min_margin` annotated with locked values + cross-reference), `11-admin-ui/06-product-add-edit-workflow.md` §3.8 (concrete floor numbers + cost-redacted warning copy clarification), `15-phases/phase-1-acceptance.md` (4 new criteria covering seed values, soft warning thresholds, cost-basis verification, profit_guardrail_log writes, cost-redacted warnings), `15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (§16 Margin floors row + §18 priority 9 → DONE). Tracker decisions **D-PAY-002** and **D-PAY-003** marked 🟢 Answered. Top 10 reshuffled — **D-LAUNCH-006 (hosting budget tier approval) promoted into rank #10** since it gates infrastructure provisioning sign-ups. Tracker stats: **139 total, 9 answered, 21 open Phase 1 blockers**.
- 2026-05-07: **Profit margin floors clarified as INITIAL DEFAULTS, not hardcoded constants.** Doc `05-payments/14-profit-floors-and-guardrails.md` updated with new §1b "Configurability principle" — 9 locked rules covering: floors live as data (`profit_floor_rule` + `country.business_min_margin` cache); CI grep test fails any commit containing literal margin values inside guardrail-evaluation code paths; floors editable from `/admin/finance/profit-guardrails` (Super Admin + Finance Admin only); raising = audit log only; lowering = approval workflow OR Manual Override; every change creates a versioned `profit_floor_rule` row + `audit_log` entry; Profit Guardrails always reads currently-active floor at evaluation time; **past orders never recalculated** — `order_line` snapshots immutable forever. New §8 fully specifies the admin Margin Settings screen, edit flow (raise vs lower), floor history view, order snapshot immutability, and per-role permission matrix. Updated: `01-database/02-tables-by-module.md` (`country.business_min_margin` annotation refreshed to "INITIAL defaults set, configurable — NOT hardcoded"), `11-admin-ui/06-product-add-edit-workflow.md` §3.8 (Profit Guardrails reads live values, never literals from code), `15-phases/phase-1-acceptance.md` (8 new acceptance criteria: no-hardcoding CI grep test, Margin Settings admin functional, raise-floor flow, lower-floor approval flow, floor history view, past-order immutability test, live-floor-read test). Tracker D-PAY-002 + D-PAY-003 entries refreshed with "INITIAL DEFAULTS, configurable" framing. **Tracker stats unchanged: 139 total, 9 answered, 21 open Phase 1 blockers** (this is a clarification, not a new answered decision).
- 2026-05-07: **D-INFRA-004 schema migration tool ANSWERED and locked: `node-pg-migrate`.** New ADR `00-architecture/adr/ADR-027-migration-tool.md` (✅ Accepted). Reasons: SQL-first; ORM-free; explicit RLS / triggers / views / indexes shipped as migration files; auditable in code review (critical for cost-privacy review under D-RBAC-001); good fit for the project's RLS, audit triggers, and dynamic country/warehouse architecture. **Locked rules:** migrations version-controlled in `/migrations/sql/`; rollback (down) section required where safe (with explicit `-- DOWN INTENTIONALLY EMPTY` comment when rollback would lose data); no migration runs in Phase 0; Phase 1+ requires explicit approval; pre-risky-change auto-backup before any production migration touching cost-bearing tables, RLS policies, or warehouse stock; staging deploys auto after CI; production manual + dual-approval (Super Admin + DBA or CTO); forward-only after Phase 1 launch; CI fails any migration that adds literal margin values, hardcoded country strings, or misses audit triggers on cost-bearing columns. Schema must remain compatible with dynamic `country_id` + `warehouse_id` per D-COUNTRY-014 / D-COUNTRY-002. Updated: ADR-027 (NEW Accepted), `00-architecture/adr/ADR-INDEX.md` (ADR-027 ✅), `00-architecture/stack-decisions.md` (Schema migration tool row Confirmed), `01-database/05-migration-plan.md` (Status banner + locked rules section + workflow commands + PR review checklist), `15-phases/phase-1-execution-plan.md` (scope item #5 references node-pg-migrate + ADR-027), `15-phases/phase-1-acceptance.md` (NEW "Schema migrations" acceptance section with 8 criteria covering rollback discipline, RLS test suite, CI gates, PR template enforcement, production workflow rehearsal, pre-risky-change backups, forward-only policy), `15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (§18 priority 10 migration tool → DONE; new priority 10 added for `order` reserved-word collision). Tracker decision **D-INFRA-004** marked 🟢 Answered. Top 10 reshuffled — **D-DB-001 (`order` reserved-word collision)** promoted into rank #10 since it's the next schema-shaping decision blocking migration 0008 (cart & order tables) now that the migration tool is locked. Tracker stats: **139 total, 13 answered, 17 open Phase 1 blockers**.
- 2026-05-07: **D-INFRA-001 backend hosting region ANSWERED and locked: Hetzner Frankfurt for Phase 1.** New ADR `00-architecture/adr/ADR-018-hosting-backend.md` (✅ Accepted). Reasons: cost-effective for MVP (~40–60% cheaper than AWS me-south-1), great Docker fit, operational simplicity, Cloudflare PoPs absorb MENA latency, easier than AWS for initial build. **AWS me-south-1 reserved as future migration option** if ZATCA mandates in-region data, latency complaints surface from KSA admin users, enterprise contracts demand hyperscaler, or Hetzner SLA insufficient. Migration plan documented in ADR-018 §"Future migration to AWS me-south-1" (RDS + ElastiCache + ECS/EKS path; dual-write verification window; DNS cutover with rollback). **Locked rules:** production secrets via `op` CLI from 1Password (no plaintext on servers); backups stay R2 primary + B2 offsite (ADR-023); frontend hosting (Vercel) decided separately in ADR-017. Topology: app + Directus admin on Hetzner CCX servers; Postgres + pgvector on dedicated/CCX; Redis + BullMQ + Meilisearch + Centrifugo on CX/CCX. Phase 1 baseline cost target ~€100–200/month (vs ~€500–1,200/month equivalent AWS). Updated: ADR-018 (NEW Accepted), `00-architecture/adr/ADR-INDEX.md` (ADR-018 ✅), `00-architecture/stack-decisions.md` (Hosting backend row Confirmed), `00-architecture/environment-topology.md` (environments table specifies Hetzner Frankfurt for dev/staging/prod backend; region rationale table updated; Open Questions hosting TODO → Answered), `15-phases/phase-1-execution-plan.md` (scope item #6 references Hetzner Frankfurt + ADR-018), `15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (§18 priority 5 → DONE; new priority 5 added for brand assets). Tracker decision **D-INFRA-001** marked 🟢 Answered. Top 10 reshuffled — **D-LAUNCH-005 (brand assets status)** promoted into rank #10 since brand tokens gate Phase 1 design system. Tracker stats: **139 total, 12 answered, 18 open Phase 1 blockers**.
- 2026-05-07: **D-BKP-001 secrets manager + D-BKP-002 backup destinations ANSWERED and locked.** **Secrets manager: 1Password Secrets Automation** (ADR-022 ✅ Accepted). 7-vault structure: `vtc-prod-payment`, `vtc-prod-whatsapp`, `vtc-prod-ai`, `vtc-prod-infra`, `vtc-prod-backup-keys`, `vtc-staging`, `vtc-dev` (test-mode keys only). Hardware-key (YubiKey) MFA mandatory on Super Admin + Finance Admin + Security Lead 1Password accounts. Single CI bootstrap secret: `OP_SERVICE_ACCOUNT_TOKEN` rotated quarterly. **Backup destinations: Cloudflare R2 primary + Backblaze B2 offsite** (ADR-023 ✅ Accepted). R2 zero-egress + same control plane as media (ADR-013); B2 different cloud → reduces correlated-failure risk + cheap cold storage. R2 → B2 replication via scheduled BullMQ worker. All payloads AES-256 encrypted with `BACKUP_ENCRYPTION_KEY` from `vtc-prod-backup-keys` 1Password vault (split-knowledge escrow Guardian A + B). Bucket layout: 7 R2 buckets (logical/WAL/snapshots/archive/media/files/pre-risky-change) + 4 B2 mirror buckets (snapshots-weekly/archive/media/files). Updated: `00-architecture/adr/ADR-022-secrets-manager.md` (Status TODO → ✅ Accepted with full vault structure), `00-architecture/adr/ADR-023-backup-destination.md` (Status TODO → ✅ Accepted with bucket layout + replication), `08-backups/01-backup-types.md` (status banner + R2/B2 buckets in matrix), `08-backups/05-secrets-exclusion-policy.md` (1Password as single source of truth + vault structure + hardware MFA), `00-architecture/environment-topology.md` (Secrets storage + Backup storage sections refreshed), `15-phases/phase-1-acceptance.md` (Backup section expanded + new Secrets management section with 7 acceptance criteria), `15-phases/PROJECT_TIMELINE_AND_PHASE_REQUIREMENTS.md` (§16 deps + §18 priorities 6-7 → DONE). Tracker decisions **D-BKP-001** and **D-BKP-002** marked 🟢 Answered. Top 10 reshuffled — **D-LAUNCH-007 (engineering team allocation)** and **D-COUNTRY-013 (inventory authority)** promoted into ranks #9 and #10. Tracker stats: **139 total, 11 answered, 19 open Phase 1 blockers**.

### TODO before Phase 0 closure
- All 80 pre-coding questions answered in writing.
- All 30 ADRs accepted and signed off.
- Stack decision checklist 100% complete.
- WhatsApp Cloud API approved for KSA + ≥6 templates approved.
- ≥1 KSA card payment provider approved.
- Backup destination buckets created and verified writable.
- Encryption key generated, escrowed, recovery drill scheduled.
- Phase 1 acceptance criteria signed off.
